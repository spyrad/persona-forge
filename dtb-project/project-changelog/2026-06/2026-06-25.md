# 2026-06-25

## Session 1 - 05:54

### Implementiert

`integration`-CI-Blocker aus Session 2026-06-24 gelöst — und die Leithypothese der Vorsession (Key-Form) als Sackgasse widerlegt.

- **Diagnose (echte Ursache statt Hypothese):** Die Vorsession vermutete den `integration`-Job-Fehlschlag in der Key-Form (`ANON_KEY` JWT vs. `sb_publishable_…` → 401). Lokal reproduziert: JWT-Key **und** publishable-Key **und** slim-Service-Set laufen alle grün (74/74) → Hypothese widerlegt. Echte CI-Logs (Schritt `npm run test:integration`) zeigten **genau einen** Fehlschlag: `run-integrity.itest.ts` → „zwei parallele steps → genau eine Repetition".
- **Root Cause:** Der Nebenläufigkeitstest erwartete genau **eine** Repetition bei zwei parallelen `processNextRepetition`-Calls. Je nach Interleaving entstehen aber 1 (eng verzahnt → beide zielen `rep_index=1` → `23505`-Catch) ODER 2 (sequenziell → `rep_index` 1 & 2) — **beide datenkonsistent** (unique `(run_id, rep_index)` schließt echte Duplikate aus). Lokal stabil 1 (grün), CI stabil 2 (rot) = Heisenbug, kein Infra-/Linux-Problem.
- **Fix (Option A — Test lockern, Produktlogik unverändert):** Test friert jetzt die ehrliche Garantie ein: kein Duplikat, keine Lücke (`rep_index == {1..n}`), kein Überschreiten von `repetition_count`, kein Throw, konsistenter Fortschritt — statt „genau eine". Beide Interleavings lokal verifiziert (1 und 2 Repetitions erzwungen, je grün).
- **Geliefert:** PR #1 (Branch `fix/run-integrity-concurrency-flake`, `1b2c0ac`) → `ci`+`integration` in echter CI grün → merge `b6c7589`. Branch lokal+remote gelöscht.
- **Abschluss Test-Rollout:** Change `testing-quality-gates-wiring` (Phase 3) via `/10x-archive` nach `context/archive/2026-06-24-testing-quality-gates-wiring/` archiviert (`bb18dee`). Damit alle 3 Rollout-Phasen aus `test-plan.md` §3 fertig.
- **Doc-Sync (`cdac07d`, `0ab2be5`, `eb01a7c`, `09d9196`):** WORKFLOW_STATUS-Stand + echte-Ursache-Gotcha; Korrektur „CI-Gate seit Phase 3 aktiv" in WORKFLOW_STATUS **und** `CLAUDE.md` (Development-Commands sagte fälschlich „Noch KEIN CI-Gate"); Session-Log.
- **Repo-Hygiene (`08b74cc`):** stale Root-`WORKFLOW_STATUS.md` (Stand 06-24, `/pf:`-Namespace, trug die widerlegte Key-Hypothese als „nächsten Schritt") gelöscht. Zwei nur dort vorhandene offene Aufgaben (OEJTS-Quelle, Repo-Description) vorher in die canonical-Datei gerettet.
- **Verifiziert:** Prod-Deploy (`deploy`-Job) auf `0ab2be5`, `09d9196` **und** `08b74cc` je grün. `main` lokal = remote = `08b74cc`, working tree clean.

### Dateien

- `src/test/integration/run-integrity.itest.ts` — Nebenläufigkeitstest auf ehrliche Eigenschaft gelockert (`toHaveLength(1)` → `{1..n}`-Invariante, Bounds, Fortschritt)
- `context/changes/testing-quality-gates-wiring/` → `context/archive/2026-06-24-testing-quality-gates-wiring/` — archiviert, `change.md` `status: archived`
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` — Stand/Meilensteine/Gotchas aktualisiert; OEJTS-Quelle + Repo-Description als offene Aufgaben übernommen
- `CLAUDE.md` — Development-Commands: „CI-Gate seit Phase 3 aktiv" statt „Noch KEIN CI-Gate"
- `WORKFLOW_STATUS.md` (Root) — **gelöscht** (stale Duplikat)

### Kontext

Der seit 2026-06-24 rote `integration`-Job hatte das `deploy`-Gate korrekt blockiert (Branch-Protection Required Checks funktionierten) — der Fehler lag im Test selbst, nicht in der Pipeline. Lehre: bei „CI rot / lokal grün" zuerst die echten Schritt-Logs ziehen (`gh run view --log-failed`; `gh` via `winget install GitHub.cli --source winget`, msstore-Quelle scheitert an TLS-Interception), bevor man eine Hypothese auf `main` pusht — jeder rote `main`-Push ist durch die Required Checks jetzt teuer.

### Naechste Schritte

- [ ] Kein aktives Feature — neues via `/dtb:feature-start`, oder Backlog-Ideen (Cross-Device E-Mail-Confirm, Custom SMTP)
- [ ] OEJTS-Items als gemeinfreie Quelle fixieren/dokumentieren (Owner: Damian) — aus Root-Status gerettet
- [ ] Repo-Description + Topics auf GitHub setzen — manueller Schritt

> Erledigt in dieser Session: Stale Root-`WORKFLOW_STATUS.md` aufgelöst (`08b74cc`); `CLAUDE.md`-CI-Gate-Stelle korrigiert (`09d9196`).

---

## Session 2 - 10:23

### Implementiert

Playwright-E2E-Lernschicht (Kurslektion s03e04) komplett aufgesetzt und nach `main` gemergt — via Brainstorming → writing-plans → subagent-driven-development (Implementer pro Task + Task-Reviews + finales Whole-Branch-Review auf Opus).

- **Scaffold + Lever:** `@playwright/test` + Chromium (Download über `NODE_OPTIONS=--use-system-ca`), `playwright.config.ts` (webServer `astro dev`, **Port 4329** statt 4321 gegen Reuse-Footgun), `seed.spec.ts` (Exemplar) + `tests/e2e/README.md` (E2E-Rules), `.env.e2e.example`, Scripts `test:e2e`/`test:e2e:ui`.
- **Auth-Setup (Ansatz A):** `auth.setup.ts` — Test-User via supabase-js anon-`signUp` (Timestamp-Mail) + echtes Formular-Login → `storageState` (`playwright/.auth/user.json`); Safety-Guard verweigert nicht-lokale `SUPABASE_URL`. `getByLabel("Password", { exact: true })` nötig (PasswordToggle-`aria-label` matcht sonst doppelt).
- **Risk-#5-Spec:** `auth-redirect.spec.ts` — Test A (unauth → `/auth/signin`) + Test B (authed → `/dashboard`); **Deliberate-Break verifiziert** (Test A wird rot, wenn `/dashboard` aus `PROTECTED_ROUTES` fällt; danach revertiert).
- **Zentrale Entscheidung (workerd-Gotcha):** Der `@astrojs/cloudflare`-Dev-Adapter überschreibt `process.env` per `Object.assign` mit `.dev.vars` (= deine **Prod**-Secrets) → die saubere `.env.e2e`+`webServer.env`-Mechanik wurde geklobbert. Erste Lösung (Runner patcht `.dev.vars`) wegen Prod-Credential-Risiko verworfen. Finale Lösung: **E2E-gated Node-Adapter** (`@astrojs/node@^10.1.4`, aktiv nur bei `process.env.E2E` via `webServer.env`) → `.dev.vars`/`.env` werden im E2E-Lauf **nie** angefasst (md5 byte-für-byte verifiziert). Normaler `dev`/`build` bleibt Cloudflare.
- **Tests:** Unit 48/48, E2E 4/4 grün. Final-Review (Opus): kein Critical/Important; Minor-Findings gefixt (URL-Regexes geankert, CLAUDE.md-Gotcha, Doku-Notizen).

### Dateien

- `playwright.config.ts` (neu), `astro.config.mjs` (E2E-gated `node()`/`cloudflare()`), `package.json`/`package-lock.json` (+`@astrojs/node`, Scripts)
- `tests/e2e/{seed,auth-redirect}.spec.ts`, `tests/e2e/auth.setup.ts`, `tests/e2e/README.md`, `.env.e2e.example`, `.gitignore`
- `context/foundation/test-plan.md` (§6.3 non-gating Notiz), `CLAUDE.md` (Gotcha: `@astrojs/node`-Astro-6-Pin in lockstep bumpen)
- `docs/superpowers/specs/2026-06-25-playwright-e2e-setup-design.md`, `docs/superpowers/plans/2026-06-25-playwright-e2e-setup.md`

### Kontext

Kurslektion s03e04 (E2E mit Playwright). Bewusst Lern-/Demo-Schicht: `test-plan.md` deferrt E2E weiterhin (alle 5 Risiken sind durch Integration-Tests abgedeckt) — die Schicht ist **kein** Deploy-Gate, nur Risk #5 (Auth-Redirect) wird im Browser demonstriert. Hauptaufwand war der workerd-`.dev.vars`-Clobber, sauber gelöst über die Adapter-Umschaltung (Prod-Secret-Isolation). Branch `feat/playwright-e2e-setup` (9 Commits) per Fast-Forward nach `main` gemergt, Branch gelöscht.

### Naechste Schritte

- [ ] `s03e05` (Debugging vom Stack-Trace) — nächste Kurslektion (mit dem `run-integrity`-Heisenbug faktisch begonnen)
- [ ] OEJTS-Items als gemeinfreie Quelle fixieren/dokumentieren (Owner: Damian)
- [ ] Repo-Description + Topics auf GitHub setzen — manueller Schritt
