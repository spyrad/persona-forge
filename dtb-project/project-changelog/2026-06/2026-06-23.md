# Changelog 2026-06-23

## Session 1 - 07:10 (Test-Rollout Phase 1: Integration Security Gate)

### Implementiert

**Test-Rollout Phase 1 (`testing-integration-security-gate`) komplett implementiert: erstes Integration-Test-Harness des Projekts + Regressionssicherungen für Risk #1/#2/#5.** Vollständige `/10x`-Kette durchlaufen (new → research → plan → implement, 4 Phasen). 5 Commits lokal (`834a3d4`→`8d0e80e`), **noch nicht gepusht**.

- **Research + Plan:** `research.md` (4 parallele Agenten: Harness-Machbarkeit, Risk #1/#2/#5-Flächen + Archiv-Historie) → Befund: Code ist an allen drei Fronten sauber, Phase 1 friert korrektes Verhalten ein. `plan.md`/`plan-brief.md`: 4 Phasen, 6 Entscheidungen (lokales Docker-Supabase, Hybrid-Harness, anon-key-signUp, volle RLS-Tiefe, `*.itest.ts`-Trennung).
- **Phase 1 — Harness-Bootstrap (`834a3d4`):** `vitest.integration.config.ts` (sequenziell, `astro:env/server`→process.env-Stub-Alias), `setup.ts` (manuelles `.env.test`-Parsing ohne dotenv-Install wg. TLS-Gotcha + Safety-Guard gegen nicht-lokale URL), `accounts.ts` (Zwei-Account-Fixture via `signUp`/Cleanup), `.env.test.example` + `.gitignore`-Fix + `test:integration`-Scripts, Smoke-Test.
- **Phase 2 — Risk #1 RLS-Matrix (`a41a90f`):** `fixtures.ts` + `rls-cross-tenant.itest.ts` — 27 Fälle über personas/runs/model_configs inkl. feiner Fälle (a) global≠write, (b) run_repetitions-Doppelpfad; DB-Gegenproben + Positiv-Kontrollen. Repetitions direkt per Client eingefügt (kein LLM-Call).
- **Phase 3 — Risk #2 Key-Dichtheit (`3c600c4`):** `key-boundary.itest.ts` — Sentinel-Key-Abwesenheit in create/list/update-Views + Run-DTO; `getDecryptedTarget` als einziger Klartext-Pfad; Compile-Zeit-Regression-Lock auf `ModelConfigView` (verifiziert: Key-Feld bricht `astro check`).
- **Phase 4 — Risk #5 Auth-Gates + Closeout (`8103330`):** `route-context.ts` (Mock-`APIContext`, In-Process-Handler-Aufruf — KEINE Astro-Container-API nötig dank Stub-Alias) + `auth-gates.itest.ts` — 17 geschützte Handler unauth→401, Auth-Routes public. Doku: test-plan.md (§3 P1→`complete`, Cookbook §6.2/§6.4 gefüllt, §6.6-Notiz), CLAUDE.md (Test-Runner-Doku korrigiert).
- **Ergebnis:** 54 Integration-Tests + 48 Unit-Tests, alle grün. Lint + `astro check` sauber.

### Dateien

- `vitest.integration.config.ts`, `src/test/integration/{setup,accounts,fixtures,route-context,smoke,rls-cross-tenant,key-boundary,auth-gates,astro-env-server.stub}.ts` — **neu**: Harness + 4 Test-Suites
- `.env.test.example`, `.gitignore` (`!.env.test.example`), `package.json` (`test:integration[:watch]`) — Test-Infra
- `context/foundation/test-plan.md` — §3 P1 `complete`, Cookbook §6.2/§6.4 gefüllt, §6.6-Phase-1-Notiz, Datum 2026-06-23
- `CLAUDE.md` — „kein Test-Runner"-Satz durch `npm run test` + `test:integration`-Doku ersetzt
- `context/changes/testing-integration-security-gate/` — change/research/plan/plan-brief (change.md → `implemented`)

### Kontext

Erster Kurs-Modul-3-Rollout-Schritt nach dem OEJTS-MVP. Das Harness fährt zwei echte authentifizierte Supabase-Sessions gegen ein lokales Docker-Supabase und sichert die drei Sicherheits-Risiken regressionsfest ab; es wird von test-plan Phase 2 wiederverwendet.

### Befunde / Gotchas

- **`astro:env/server` ist die einzige Astro-Kopplung** der Service-Schicht — ein Vite-Alias auf einen `process.env`-Stub löst sie für Service- UND Route-Level-Tests; die schwerere Astro-Container-API war unnötig (direkter Handler-Import mit Mock-`APIContext` genügt).
- **Lokale DB braucht `npx supabase db reset`:** `supabase start` allein hatte die 8 Migrationen nicht angewandt (`public.model_configs` fehlte) — reset spielt sie sauber ein. Kein `seed.sql` vorhanden → feiner RLS-Fall „Seed owner=NULL" nicht testbar (per anon-key nicht erzeugbar), in den global-Objekt-Fällen mit abgedeckt.
- **Resume-Fehlleitung:** Dieser Session-Resume las die **stale Root-`WORKFLOW_STATUS.md`** (06-13) statt der canonical `dtb-project/project-workflows/WORKFLOW_STATUS.md` (06-22) → anfänglich falscher Stand, via Changelog/`git ls-remote` korrigiert. Das Duplikat sollte endlich aufgelöst werden.

### Naechste Schritte

- [ ] **`! git push`** — 5 Commits (`834a3d4`→`8d0e80e`) lokal, remote `main` steht auf `16e305c`. Diff ist Test-Code + `context/`/`dtb-project/`-Doku (kein Worker-Runtime-Effekt).
- [ ] Optional: `/10x-impl-review testing-integration-security-gate` (Voll-Review) vor Archivierung
- [ ] `/10x-archive testing-integration-security-gate` — Slice schließen
- [ ] test-plan **Phase 2** (Run-Integrität + SSRF, Risk #4+#3) via `/10x-new testing-run-integrity-ssrf` anstoßen
- [ ] test-plan **Phase 3** (CI-Gate: `npm run test` als Pre-Deploy-Gate) — schließt „CI-Lint-Fail skippt Deploy lautlos"
- [ ] Stale Root-`WORKFLOW_STATUS.md` (06-13) auflösen — canonical ist `dtb-project/project-workflows/WORKFLOW_STATUS.md`

---

## Session 2 - 18:18 (Push, Impl-Review & Archivierung Phase 1)

### Implementiert

**Test-Rollout Phase 1 (`testing-integration-security-gate`) gepusht, voll-reviewt (APPROVED) und archiviert.** Der Slice ist damit final geschlossen; `main` lokal = remote = `0bdc87e`.

- **Push Phase-1-Implementierung (`68a332f`):** Die 5 Slice-Commits aus Session 1 (`834a3d4`→`8d0e80e`) plus Doku-Commit nach `main` gepusht (`16e305c`→`68a332f`). CI-Lauf `28036894380` verifiziert — Jobs `ci` + `deploy` beide grün, `cloudflare/wrangler-action@v3` tatsächlich ausgeführt (kein lautloser Deploy-Skip).
- **Impl-Review (`/10x-impl-review`):** Full-Plan-Review über alle 4 Phasen via zwei parallele Agenten (Plan-Drift + Safety/Quality/Pattern). Verdikt **APPROVED** — alle 6 Dimensionen PASS. Bestätigt: kein Produktcode berührt, kein `service_role`-Key, kein CI-Gate (alle „NOT doing"-Grenzen eingehalten); S-02-Anti-Pattern (leeres Array als Beweis) konsequent vermieden; Auth-Checks laufen echt durch; Key-Dichtheit beweiskräftig + compile-zeit-gelockt.
- **F1-Fix (Safety-Guard-Härtung):** `setup.ts`-Guard von Substring-Match (`/127\.0\.0\.1|localhost/`) auf echten Host-Parse (`new URL(url).hostname`) umgestellt — blockt jetzt `localhost.evil.com` & Co. 4 Observations (F2–F5) bewusst geskippt. Commit `7ce4b5e`, CI-Lauf `28038800699` (ci + deploy grün) verifiziert.
- **Archivierung (`/10x-archive`):** Change-Ordner per `git mv` nach `context/archive/2026-06-23-testing-integration-security-gate/` verschoben (History folgt), `change.md` → `status: archived` + `archived_at`. Kein Roadmap-Treffer. Commit `0bdc87e`, gepusht.

### Dateien

- `src/test/integration/setup.ts` — F1-Fix: Host-Parse-Guard statt Substring (`7ce4b5e`)
- `context/archive/2026-06-23-testing-integration-security-gate/` — kompletter Change-Ordner (verschoben aus `context/changes/`), inkl. neuem `reviews/impl-review.md` (APPROVED-Report)
- `context/archive/.../change.md` — `status: archived`, `archived_at: 2026-06-23T16:00:56Z`

### Kontext

Abschluss-Session zu Test-Rollout Phase 1: die in Session 1 implementierte Integration-Security-Gate wurde deployt, unabhängig reviewt (sauber, ein Low-Severity-Fix) und nach `context/archive/` geschlossen. Das Zwei-Account-Integration-Harness steht damit produktiv bereit und wird von Phase 2 wiederverwendet.

### Befunde / Gotchas

- **CI-Verifikation bestätigt 2×:** Nach beiden Code-Pushes lief der `deploy`-Job inkl. wrangler-action tatsächlich durch — der „CI-Lint blockt deploy lautlos"-Gotcha hat diesmal nicht gegriffen (Lint grün).
- **Impl-Review-Wert:** Der einzige substanzielle Befund (F1) war ein echtes, wenn auch niedrigschwelliges Sicherheits-Hardening am Test-Harness selbst — der Substring-Guard hätte theoretisch Tests gegen eine Prod-URL mit `localhost`-Substring durchgelassen.

### Naechste Schritte

- [ ] **Rollout-Phase 2** (R4 Lauf-Integrität + R3 SSRF) via `/10x-new testing-run-integrity-ssrf` — siehe `test-plan.md` §3.
- [ ] **Rollout-Phase 3** (`npm run test` als CI-Pre-Deploy-Gate) — schließt „CI-Lint-Fail skippt Deploy lautlos".
- [ ] Stale Root-`WORKFLOW_STATUS.md` (06-13) auflösen — canonical ist `dtb-project/project-workflows/WORKFLOW_STATUS.md`.

---

## Session 3 - 22:12 (Test-Rollout Phase 2: Run-Integrität + SSRF — komplett + archiviert)

### Implementiert

**Test-Rollout Phase 2 (`testing-run-integrity-ssrf`) komplett: R4 (Lauf-Integrität) + R3 (SSRF-Boundary) regressionsfest abgesichert.** Volle `/10x`-Kette (new → research → plan → implement 4 Phasen → impl-review → archive). 8 Commits (`d23c477`→`fe87873`), gepusht; `ci`-Job grün.

- **Research:** 4 parallele Agenten — beide Risikoflächen sauber im Code verankert (Phase 2 friert ein, repariert nicht). Kernbefund: SSRF-Guard ist an ZWEI unabhängigen Call-Sites verdrahtet (kein gemeinsamer Wrapper); Step-Idempotenz via Unique `(run_id,rep_index)` + 23505-Catch; Abort = DB-`on delete cascade`.
- **Plan:** 4 Phasen, MEDIUM-Komplexität, 5 Solution-Design-Entscheidungen (fetch-Kanten-Mock, Idempotenz Terminal+Nebenläufig, repräsentative SSRF-Payloads, shared Fixtures, SSRF-URL + 3xx-Redirect).
- **Phase 1 — Harness (`d23c477`):** `makePendingRun`/`makeRunningRun`/`makeFailedRun` in `fixtures.ts`; `llm-mock.ts` (neu) fetch-Kanten-Mock; Smoke-Asserts. **Bug gefunden:** `vi.stubGlobal("fetch")` trifft auch supabase-js → Mock reicht lokale Calls durch, mockt nur die LLM-Kante.
- **Phase 2 — R4 (`efbc8c3`):** `run-integrity.itest.ts` — Abort/Cascade-Vollständigkeit, Idempotenz (Terminal + Promise.all-23505, 5× stabil), Failure-Quote/kein partielles Aggregat.
- **Phase 3 — R3 (`1b02edb`):** `ssrf-boundary.itest.ts` — beide Call-Sites + 3xx-Redirect je Site. **Harness-Erweiterung:** `authedCookieHeader` (via `@supabase/ssr`) ermöglicht erstmals authentifizierte In-Process-Route-Aufrufe (schließt test-plan §6.4-„manuell"). Guard-Entfernungs-Probe: 5 Tests rot ohne Guard.
- **Phase 4 — Closeout (`cb563e8`):** test-plan §3 P2 → complete, §6.5 Cookbook gefüllt, §6.6-Notiz; Epilog (`4882247`).
- **Impl-Review (`8913a93`):** APPROVED, alle 6 Dimensionen PASS; 3 Low-Impact-Observations gefixt (Hostname- statt Substring-Pass-through, `requireEnv` statt `?? ""`, Plan-Helper-Name).
- **Archiviert (`fe87873`):** `git mv` → `context/archive/2026-06-23-testing-run-integrity-ssrf/`, `change.md` → `archived`.
- **Ergebnis:** 74 Integration-Tests (vorher 54) + 48 Unit-Tests grün. Kein Produktcode berührt.

### Dateien

- `src/test/integration/{fixtures,route-context,smoke.itest}.ts` — erweitert (Run-Builder, `authedCookieHeader`, Smoke)
- `src/test/integration/{llm-mock,run-integrity.itest,ssrf-boundary.itest}.ts` — **neu**
- `context/foundation/test-plan.md` — §3 P2 `complete`, §6.5/§6.6 gefüllt
- `context/archive/2026-06-23-testing-run-integrity-ssrf/` — kompletter Change-Ordner (research/plan/plan-brief/reviews/change)

### Kontext

Zweiter Kurs-Modul-3-Rollout-Schritt. Nutzt das Phase-1-Zwei-Account-Harness wieder und ergänzt es um Run-Builder, einen fetch-Kanten-Mock (mit Supabase-Pass-through) und authentifizierte In-Process-Route-Aufrufe. Sichert die beiden verbleibenden Top-Risiken (Lauf-Integrität, SSRF) regressionsfest ab.

### Befunde / Gotchas

- **`vi.stubGlobal("fetch")` trifft supabase-js** (nutzt intern global fetch) — Mock muss lokale Calls (Host `127.0.0.1`/`localhost`) durchreichen, nur die Outbound-Kante mocken.
- **SSRF-Guard an 2 Sites**, kein gemeinsamer Wrapper → jede separat getestet; im Run-Step wirft er VOR dem fetch (daher dort kein Mock nötig).
- **`authedCookieHeader`**: echte Session als Cookie via `@supabase/ssr` selbst erzeugt (kompatibles, ggf. gechunktes Format) — `createClient` liest aus dem Request-Header, nicht aus dem cookies-Adapter.
- **23505-Nebenläufigkeit** braucht `completedReps=0`, sonst verschiebt sich `rep_index` und der Catch wird verfehlt. Test deterministisch (Read-Committed).

### Naechste Schritte

- [ ] **Rollout-Phase 3** (`npm run test` als CI-Pre-Deploy-Gate) via `/10x-new` — schließt „CI-Lint-Fail skippt Deploy lautlos" (test-plan §3 letzte Phase).
- [ ] Stale Root-`WORKFLOW_STATUS.md` (06-13) auflösen — canonical ist `dtb-project/project-workflows/WORKFLOW_STATUS.md`.
