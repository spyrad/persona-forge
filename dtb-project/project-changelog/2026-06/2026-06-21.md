# Changelog 2026-06-21

## Session 1 - 08:30

### Implementiert

**S-07 `visibility-controls` von Phase-1-Backend (impl, uncommittet) bis vollständig abgeschlossen: Migration angewandt → Phase 1 verifiziert+committet → Phase 2 (UI) → Phase 3 (Zwei-Account-Gate) → impl-review APPROVED → Roadmap `done` → archiviert. Slice geschlossen, 10 Commits lokal vor `origin/main` (noch nicht gepusht).**

- **Migration auf Prod-DB:** `npx supabase db push` (User-`!`) spielte `20260620092033_personas_update_own_policy.sql` ein → entsperrt den Persona-PATCH (Dev-Server läuft gegen Prod-Supabase).
- **Phase 1 (Backend) manuell verifiziert (1.6–1.9)** autonom via Playwright-MCP (Testaccount A `md.motion.value@gmail.com`, authentifizierter Browser-`fetch` gegen Dev-Server):
  - Persona-PATCH eigen → 200 (`global` ↔ restore `private`); Run-PATCH eigen → 200 (`private` ↔ `global`).
  - Fremd-Persona-PATCH → 404, Random-Run-uuid-PATCH → 404, invalider Body → 400.
  - Create-Default: neue Persona + neuer Lauf tragen `visibility:"global"` (FR-003).
  - Commits `6a5fd4e` (feat Backend) + `a2cb0d8` (SHA-Rückschrieb).
- **Phase 2 (UI) gebaut + verifiziert (2.4–2.7):**
  - `PersonaCatalog.tsx`: Globe/Lock-Toggle + „Privat"-Badge für eigene Personas (PATCH + Refetch, `busyId`-Loading-Lock); bestehendes Global-Badge bleibt.
  - `RunRunner.tsx`: Global/Privat-Badge + Toggle in der Lauf-Liste (nur eigene, nicht-aktive Läufe).
  - `RunResult.tsx`: read-only Sichtbarkeits-Badge im Detail-Header.
  - Verifikation: 1-Rep-Lauf (Skeptiker/gpt-4o) bis `completed` getrieben; Toggle-Round-Trips Persona+Lauf grün, RunResult-Badge korrekt. Testdaten aufgeräumt.
  - Commits `cc8676a` (feat UI) + `8ec2b8e` (SHA-Rückschrieb).
- **Phase 3 (Zwei-Account-Cross-Visibility-Gate, 3.1–3.4):** zweiter Account B (`damian.spyra@googlemail.com`) per Auth-API im Playwright-Browser eingeloggt (sequenzieller Account-Wechsel, da gemeinsamer Cookie-Jar):
  - B sieht A's globale Persona+Lauf (`isOwn:false`, kein Toggle); A's private Inhalte für B unsichtbar; B besitzt keine Gate-Fixtures (= echt anderer User).
  - B-`PATCH` auf A's globale Persona/Lauf → **404** (RLS blockt Update trotz Select-Sichtbarkeit).
  - Dynamisch: A schaltet global→privat → bei B verschwinden beide.
  - Gesamtlauf grün (48 Vitest / Lint / `astro check` 0 err / Build). Gate-Testdaten gelöscht, A-Session wiederhergestellt.
  - Commits `cf463cd` (Gate verifiziert) + `6ae555e` (SHA-Rückschrieb).
- **impl-review** (`/10x-impl-review`, 2 Parallel-Agenten Plan-Drift + Safety/Pattern): **APPROVED**, 0 critical/warning, 4 Observations (kosmetisch). Plan-Adherence sehr hoch, alle Guardrails respektiert, FR-008-Immutability gewahrt, RLS-Kern verifiziert. `be6f872`.
- **Slice geschlossen:** Roadmap S-07 → `done` + Done-Eintrag mit Lesson (`8ebaff2`); `/10x-archive` → `context/archive/2026-06-20-visibility-controls/` (`4d662dd`, `git mv` am Windows-Lock → `Move-Item`-Fallback).

### Dateien

- `supabase/migrations/20260620092033_personas_update_own_policy.sql` — `personas_update_own`-RLS-Policy (Prod-DB appliziert)
- `src/lib/services/personas.ts`, `src/lib/services/runs.ts` — `update*Visibility`; Create-Default → global
- `src/pages/api/personas/[id].ts`, `src/pages/api/runs/[id].ts` — PATCH (Sichtbarkeit)
- `src/components/personas/PersonaCatalog.tsx`, `src/components/runs/RunRunner.tsx`, `src/components/runs/RunResult.tsx` — Toggle + Badges
- `context/archive/2026-06-20-visibility-controls/` — archivierter Slice (plan, change, reviews/impl-review)
- `context/foundation/roadmap.md` — S-07 `done` + Done-Eintrag mit Lesson

### Kontext

S-07 ist der erste Slice von Stream C (Teilen & Vergleichen), joined Stream B bei S-05. Wie S-06 eine ehrliche Scope-Reduktion: S-03/S-04 hatten Enum, Spalten, RLS `select own_or_global`, `runs_update_own`, Types bereits gelegt — S-07 lieferte nur Toggle/Default/UI + das Cross-Visibility-Gate. Stream C ist damit angebrochen; nächster Slice S-08 `side-by-side-comparison` (Prereq S-05 ✅).

### Befunde / Gotchas

- **Transiente Dev-Server-500/leere SSR-Seiten bei Vite-Dep-Re-Optimierung:** Run-PATCH→global gab einmal 500, deckungsgleich mit `✨ new dependencies optimized: zod`; die Signin-Seite renderte 0 Bytes wegen `useFormStatus`-Hook-Crash während `astro/env/runtime`-Reload (zwei React-Kopien). **Environmental, kein Diff-Bug** — nach Re-Optimierung sauber. Lehre: bei E2E gegen `npm run dev` Re-Optimierungen abwarten; Auth-Wechsel besser über die Auth-**API** (`POST /api/auth/signin` FormData) als über die crashende SSR-Form-Seite.
- **Zwei-Account-RLS-Test braucht beide Passwörter:** Der Playwright-Browser teilt einen Cookie-Jar — B einloggen verdrängt A. Ohne A's Passwort kein Rückweg → A's Gate-Fixtures (owner-RLS) nicht löschbar. Beide Accounts haben dasselbe Passwort `Dupadupa19`. Sequenzieller Wechsel A→B→A funktioniert.
- **`git mv` am Windows-Lock:** wie bekannt → `Move-Item` + `git add -A` (Git erkennt Renames sauber).
- **RLS-NULL-Semantik schützt globale Seeds:** `personas_update_own` mit `owner_id = (select auth.uid())` blockt globale Seed-Personas (owner_id NULL) automatisch, weil `NULL = uid` SQL-NULL (nicht true) ist — nicht nur fremde Owner.

### Naechste Schritte

- [ ] **Push** der 10 Commits via `! git push` (Prod-Deploy; Auto-Mode blockt Push auf `main`). Danach **CI-Deploy-Job prüfen** (Lint-Fehler skippt Deploy lautlos). Migration ist bereits auf Prod-DB.
- [ ] **S-08 `side-by-side-comparison`** starten (`/10x-plan side-by-side-comparison`; Prereq S-05 ✅) — Zwei-Läufe-Vergleich nebeneinander.
- [ ] Altlasten: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren.

---

## Session 2 - 08:45

### Implementiert

**S-07 gepusht: `160ee06..5c2b5f8` → `origin/main` (11 Commits, via Damians `! git push`).** Cloudflare-Auto-Deploy getriggert. Working tree clean, `HEAD == origin/main == 5c2b5f8`.

### Kontext

Abschluss der S-07-Session: die 11 lokalen Commits (S-07 Phase 1–3 + impl-review + Roadmap-done + Archiv + Checkpoint S1) sind jetzt auf `main` und damit live-deployt. Migration `personas_update_own` war bereits separat auf der Prod-DB (`db push` in S1) — Worker-Deploy zieht keine Migration.

### Befunde / Gotchas

- **Deploy-Job nicht direkt verifizierbar:** `gh`/`GITHUB_TOKEN` fehlen lokal. S-07 fügt eine neue PATCH-Methode an bestehende Routen + UI hinzu (keine neue Route); ein prod-seitiger Fingerprint-Check ist umständlich. Verifikation steht aber über die volle Dev-Server-Treibung (Phasen 1–3) — der Code auf `main` ist identisch. CI-Lint-Fehler skippt Deploy lautlos → bei Gelegenheit im Actions-/Cloudflare-UI sichten.

### Naechste Schritte

- [ ] **S-08 `side-by-side-comparison`** (`/10x-plan side-by-side-comparison`; Prereq S-05 ✅) — letzter geplanter MVP-Slice.
- [ ] Optional: S-07-Deploy-Erfolg im Cloudflare-Dashboard / GitHub-Actions-UI sichten.
- [ ] Altlasten: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren.

---

## Session 3 - 11:17

### Implementiert

**S-07-Deploy nachträglich verifiziert + beide offenen Altlasten (Husky-Hook, F6 Trigger-Idempotenz) abgearbeitet. Commit `0df2b99` lokal (1 vor `origin/main`, noch nicht gepusht).**

- **S-07-Deploy bestätigt erfolgreich** (Altlast „Deploy sichten" erledigt): `gh` fehlt lokal → über die **GitHub-REST-API** (`curl.exe --ssl-no-revoke`, public Repo) **CI run #30** (HEAD `52e48f5`) geprüft. Beide Jobs `success`: Job `ci` (`npm run lint` ✅ → `npm run build` ✅) **und** Job `deploy` (`cloudflare/wrangler-action@v3` ✅, 07:58:58Z). Deploy-Job wurde **nicht** stumm übersprungen. Live-Check `persona-forge.damian-spyra-ai.workers.dev` → **HTTP 200**. Lokaler HEAD `== origin/main == 52e48f5`.
- **Husky/lint-staged-Hook repariert:** Ursache war ein **fehlendes `"prepare": "husky"`** in `package.json` → `core.hooksPath` blieb nach jedem `npm ci` (lokal + CI) ungesetzt, lokale Commits umgingen `lint-staged` still. Fix: `prepare`-Script ergänzt + `npm run prepare` → `core.hooksPath = .husky/_`. **Verifiziert beim echten Commit `0df2b99`**: `lint-staged`/`prettier --write` lief sichtbar mit.
- **F6 Trigger-Idempotenz:** neue Migration `20260621090619_profiles_trigger_idempotent.sql` — `handle_new_user()` mit `on conflict (id) do nothing` + `drop trigger if exists on_auth_user_created` vor `create trigger` (Re-Run-sicher). Bestehende `20260614174810`-Migration bewusst nicht editiert. Via User-`! npx supabase db push` auf Prod-DB appliziert.
- **Remote-Migrationsstand verifiziert** (`npx supabase migration list`): alle 8 Migrationen `Local == Remote`; insb. die zwei heutigen Pushes `20260620092033` (S-07 `personas_update_own`) **und** `20260621090619` (F6) beide remote vorhanden (Sorge wg. evtl. fehlendem „Y" beim ersten Push ausgeräumt).

### Dateien

- `package.json` — `"prepare": "husky"` in `scripts` (Husky-Aktivierung bei `npm ci`)
- `supabase/migrations/20260621090619_profiles_trigger_idempotent.sql` — **neu**, idempotenter profiles-Trigger (F6, remote appliziert)

### Kontext

Aufräum-Session nach S-07: die drei „Optional/Altlasten"-Punkte aus S2 abgearbeitet — Deploy-Verifikation nachgeholt und die zwei Tech-Debt-Items (Husky, F6) geschlossen. Kein Feature-Code, reine Infrastruktur-/Migrations-Hygiene.

### Befunde / Gotchas

- **Husky war seit Projektbeginn nie aktiv:** ohne `prepare`-Script aktiviert sich Husky v9 nicht → `core.hooksPath` leer → pre-commit feuerte nie. Erklärt, wie Lint-/Format-Abweichungen überhaupt bis in den CI durchrutschen konnten (lokale Vorabprüfung lief nicht).
- **GitHub-Actions ohne `gh`:** Status via REST-API `/actions/runs` + `/runs/{id}/jobs` (curl, public Repo, unauth). Wichtig: Run-Conclusion `success` allein schließt einen **geskippten** Deploy-Job nicht aus → Jobs einzeln prüfen (deploy-Job + `wrangler-action`-Step müssen `success` sein).
- **Stale-Duplikat WORKFLOW_STATUS.md:** Root-Datei (Stand 2026-06-13) ist veraltet; kanonisch ist `dtb-project/project-workflows/WORKFLOW_STATUS.md` (= `config.paths.workflows`). CLAUDE.md listet aber die Root-Datei als „Aktueller Workflow-Stand" → Widerspruch, noch zu klären.

### Naechste Schritte

- [ ] **`! git push`** (Commit `0df2b99` → `main`; Cloudflare-Auto-Deploy). Diff ist nur das `prepare`-Script (build-/dev-seitig, kein Worker-Runtime-Effekt) + Migrationsdatei (remote schon appliziert). Danach **CI-Deploy-Run prüfen**.
- [ ] **S-08 `side-by-side-comparison`** starten (`/10x-plan side-by-side-comparison`; Prereq S-05 ✅) — letzter geplanter MVP-Slice.
- [ ] **Stale-Duplikat klären:** Root-`WORKFLOW_STATUS.md` löschen oder als kanonisch festlegen + CLAUDE.md/Config angleichen.

---

## Session 4 - 21:30

### Implementiert

**S-08 `side-by-side-comparison` vollständig implementiert (3 Phasen, geplant → impl-reviewt APPROVED) — der letzte geplante MVP-Slice. 5 Commits lokal vor `origin/main`, noch nicht gepusht.**

- **Geplant** (`/10x-plan side-by-side-comparison`): MEDIUM, 3 Phasen. 7 Design-Entscheidungen via Q&A — Auswahl-Haken in der Liste, pro Achse überlagerte Darstellung, Label = Persona+Modell+Datum+Typ, nur completed+ready vergleichbar, zwei beliebige verschiedene Läufe, Typ-Vergleich + Mittelwert-Delta, bookmarkbare URL. Kein neues Datenmodell/Migration/API — reine Lese-Erweiterung auf der S-05-Aggregation.
- **Phase 1** (`b88b229`): `RunRunner.tsx` — `compareIds`-State (Cap 2) + Vergleichs-Haken je completed-Lauf + sticky Leiste → `/runs/compare?a=&b=`; Auswahl-Cleanup bei refetch. Per Playwright verifiziert (max. 2, dritter gesperrt, Navigation, Auswahl aufheben; echter 1.5-Negativtest mit pending-Lauf → kein Haken).
- **Phase 2** (`d773386`): neue `compare.astro` — lädt beide Läufe RLS-gescoped via `getRunResult` (`Promise.all`), Guard-Maschine (missing/same/notFound→404/notReady/loadError), Label-Auflösung mit `(gelöscht)`/`(unbekannt)`-Fallbacks; Typen `RunComparisonSide`/`RunComparisonView`. Edge-Cases empirisch geprüft (gleiche ID, unbekannte UUID→404, notReady, **echte gelöschte Persona → Fallback** via Wegwerf-Persona+Lauf).
- **Phase 3** (`41e63c2`): Chart-Primitive `axis-chart.tsx` (`toPct` + `AxisChart` für 1–2 Serien) aus `RunResult.tsx` extrahiert (kein Regress); `RunComparison.tsx` — zwei Köpfe, Typ-Banner (gleich/unterschiedlich), pro Achse überlagerter Chart (lila A / cyan B, beide Mittelwert-Marker) + Mittelwert-Delta A−B, Legende. Per Screenshot verifiziert: Einzelansicht unverändert, Überlagerung + Deltas (-4.4/-2.4/+1.8/-0.8) korrekt, „nicht belastbar (n<2)", responsive einspaltig.
- **impl-review** (`/10x-impl-review`, 2 Parallel-Agenten): **APPROVED**, 0 critical/warning, 3 Observations. F1 (statisch statt `client:load`) als Lesson festgehalten; F2 (DRY `RELIABLE_MIN` → `axis-chart.tsx`) gefixt; F3 (wünschenswerte Zusätze) als Stärken akzeptiert. Review-Commit `70153cd`.

### Dateien

- `src/components/runs/RunRunner.tsx` — Vergleichs-Auswahl (Haken, sticky Leiste, `navigateToCompare`)
- `src/pages/runs/compare.astro` — **neu**, SSR-Vergleichsseite + Guards (statische Route, schattet `/runs/[id]` ab)
- `src/types.ts` — `RunComparisonSide` / `RunComparisonView`
- `src/components/runs/axis-chart.tsx` — **neu**, geteilte Chart-Primitive + zentrales `RELIABLE_MIN`
- `src/components/runs/RunComparison.tsx` — **neu**, überlagerte Darstellung + Typ-Banner + Delta
- `src/components/runs/RunResult.tsx` — nutzt die geteilte Primitive (kein Regress)
- `context/foundation/lessons.md` — **neu**, erste Lesson (F1: präsentationale Astro-Inseln ohne `client:load`)
- `context/changes/side-by-side-comparison/` — plan, plan-brief, change.md (impl_reviewed), reviews/impl-review.md

### Kontext

Letzter geplanter MVP-Slice (Stream C). Wie S-06/S-07 eine ehrliche Scope-Reduktion: S-05 lieferte `getRunResult`/`RunAggregate` + Achsen-Visualisierung bereits — S-08 brauchte nur Auswahl-UI, eine SSR-Seite und die Überlagerung. Damit ist der OEJTS-MVP funktional komplett (S-01–S-08).

### Befunde / Gotchas

- **Neues Astro-Top-Level-Route-File → workerd-Dev-Server-Neustart nötig:** Nach Anlegen von `compare.astro` lief der laufende Dev-Server server-weit auf 500 / „Network connection lost" (Route-Manifest wird nicht heiß neu generiert). Neustart (`npm run dev`) behebt es.
- **Neue lucide-Imports → Vite-Dep-Re-Opt-Hydration-404:** Neue Icons triggern lucide-react-Re-Optimierung; der ausgelieferte `?v=`-Hash 404t, die React-Island scheitert an der Hydration. Gelöst, indem die **rein präsentationale** `RunComparison` ohne `client:load` statisch gerendert wird (kein Client-JS, kein Hydration) — als Lesson in `lessons.md` festgehalten.
- **Pre-Commit-Hook aktiv:** Husky/lint-staged feuerte bei jedem Commit (Prettier formatierte Tabellen/Klassen; `prettier-plugin-tailwindcss` trimmt Leerzeichen in Klassen-String-Literalen → `cn()` statt manueller Verkettung nutzen).

### Naechste Schritte

- [ ] **`! git push`** (5 Commits `b88b229..70153cd` → `main`; Cloudflare-Auto-Deploy). Danach CI-Deploy-Job prüfen (Lint-Fehler skippt Deploy lautlos).
- [ ] **Roadmap S-08 → `done`** + Done-Eintrag mit Lesson, dann **`/10x-archive side-by-side-comparison`** (OEJTS-MVP funktional komplett).
- [ ] Optional: Dev-Server-Gotchas (neue Route → Restart; lucide-Re-Opt) in die `dtb-project`/CLAUDE.md-Gotchas übernehmen.
