# Changelog 2026-06-22

## Session 1 - 05:17

### Implementiert

**S-08 `side-by-side-comparison` Close-out: Slice archiviert + Roadmap auf `done` geschlossen. Damit ist der OEJTS-MVP (S-01–S-08) auch im Tracking vollständig abgeschlossen.** 1 Commit lokal (`6d3c825`), noch nicht gepusht.

- **Resume-Korrektur:** WORKFLOW_STATUS (Stand S4) meldete „5 Commits ungepusht / archive offen" — via `git ls-remote origin` verifiziert, dass remote `main` bereits auf `01e755d` steht. **Alle S-08-Commits inkl. Checkpoint S4 waren längst gepusht & deployt**; der Status war nur vor dem finalen Push geschrieben. Einziger echter Rest war der Slice-Close-out.
- **`/10x-archive side-by-side-comparison`:** Pre-Flight grün (keine uncommitteten/staged Änderungen, impl-review vorhanden, DEST frei, keine Pending-Progress, alle Done-Zeilen mit SHA → keine Soft-Warnings). `change.md` gestempelt (`status: archived`, `archived_at: 2026-06-22T03:15:06Z`), Ordner via `git mv` nach `context/archive/2026-06-21-side-by-side-comparison/` (4 Dateien, History folgt).
- **Roadmap geschlossen** (`context/foundation/roadmap.md`): S-08 → `done` in „At a glance"-Tabelle + Slice-Body; Done-Eintrag mit Lesson ergänzt (F1: präsentationale Astro-Inseln ohne `client:load` statisch rendern wg. lucide-Re-Opt-Hydration-404; Slice kollabierte dank S-05-Vorarbeit auf reine Lese-Erweiterung). `updated: 2026-06-22`.
- **Commit `6d3c825`** `chore(archive): close side-by-side-comparison` (5 Dateien: 4 Renames + Roadmap). Pre-Commit-Hook (Prettier) hat die Roadmap-Markdown-Tabellen mitformatiert — eingeschlossen.

### Dateien

- `context/archive/2026-06-21-side-by-side-comparison/` — **archivierter Slice** (change.md gestempelt, plan, plan-brief, reviews/impl-review) — verschoben aus `context/changes/`
- `context/foundation/roadmap.md` — S-08 `done` (Tabelle + Body), Done-Eintrag mit Lesson, `updated: 2026-06-22`

### Kontext

Reine Workflow-Hygiene-Session nach dem S-08-Abschluss (S4). Kein Feature-Code. Der OEJTS-MVP ist funktional seit S4 komplett; diese Session schließt nur das Tracking nach (Roadmap + Archiv), nachdem sich herausstellte, dass der vermeintliche „Push offen"-Rest bereits erledigt war. Roadmap führt jetzt alle 8 Slices (S-01–S-08) + 2 Foundations (F-01/F-02) als `done`.

### Befunde / Gotchas

- **Stale WORKFLOW_STATUS nach Session-Ende:** Der Status wurde in S4 vor dem finalen Push geschrieben und nie nachgezogen → Resume meldete fälschlich offenen Push. Lehre: bei Resume immer `git ls-remote origin` gegen den lokal getrackten `origin/main`-Ref prüfen, statt dem Status-Doc zu vertrauen.
- **`git mv` lief diesmal sauber** (kein Windows-Lock wie bei S-07) — kein `Move-Item`-Fallback nötig.

### Naechste Schritte

- [ ] **`! git push`** für `6d3c825` → `main`. Diff ist nur `context/`-Doku/Archiv (kein Worker-Runtime-Effekt) → effektiv No-Op-Redeploy; Push auf `main` braucht User-`!`-Kommando. Danach optional CI-Deploy-Job sichten.
- [ ] Optional: Dev-Server-Gotchas (neue Route → workerd-Restart; lucide-Re-Opt → Hydration-404) in `dtb-project`/CLAUDE.md-Gotchas übernehmen (stehen aktuell nur im Changelog/`lessons.md`).
- [ ] Optional: Stale-Duplikat klären — Root-`WORKFLOW_STATUS.md` (Stand 06-13) vs. kanonisch `dtb-project/project-workflows/WORKFLOW_STATUS.md`; CLAUDE.md angleichen.
- [ ] MVP komplett → nächster Cycle: neues Instrument (Likert/Mini-IPIP, Parked) oder andere Backlog-Themen mit `/10x-shape` anstoßen.

---

## Session 2 - 05:53 (Test-Plan / Modul 3 Start)

### Implementiert

**Kurs-Modul 3 (Tests) angestoßen: `/10x-test-plan` Phase 0–4 durchlaufen, `context/foundation/test-plan.md` geschrieben, Rollout-Phase 1 eröffnet.** Kein Feature-Code — QA-Spec + Rollout-Strategie.

- **`/10x-course-status`:** Kurs-GPS erstellt. Modul 1+2 ✅ (alle MVP-Slices archiviert), Modul 3 (Tests) ist die aktive Front: Unit-Tests existieren bereits out-of-order (6 vitest-Files in `src/lib/`, alle reine Logik), aber `test-plan.md` fehlte, kein Integration-/API-Test, kein E2E. **Abweichung gefunden:** CLAUDE.md notiert „kein Test-Runner eingerichtet" — veraltet, `vitest@^4.1.9` ist installiert + `test_command` gesetzt.
- **`/10x-test-plan` Discovery (Phase 1):** Test-Basis als `sparse` profiliert (6 Files, alle `src/lib/`, gesamte API-/Service-/Auth-Schicht nackt). Hot-Spot-Scan `src/` (31 Commits/30d): Top-Dirs `src/lib/services/` (14), `src/components/runs/` (11), `src/pages/api/`. Grounding-Tools: Context7, Playwright MCP, Supabase verfügbar; Exa nicht.
- **Interview (Phase 2):** Top-Angst = API-Key-Klartext-Leak in Response/Bundle (Q1+Q4); häufigste unsichere Änderung = RLS/Sichtbarkeit cross-tenant (Q3); noch nirgendwo produktiv gebrannt (Q2); Negativ-Raum unsicher (Q5) → Vorschlag akzeptiert.
- **5 Top-Risiken (Phase 3, Abuse-Linse Pflicht):** R1 Cross-Tenant-Leak (High×High), R2 Key-Boundary-Leak (High×Med), R3 SSRF an Request-Boundary (High×Med), R4 Lauf-Integrität/Idempotenz/Abbruch (High×Med-High), R5 Auth-Route-Schutz (High×Med). Deterministisches Scoring + isolierte Helper als „schon abgedeckt" markiert (kein Risiko-Row).
- **3 Rollout-Phasen (§3):** P1 Integration-Security-Gate (R1+R2+R5, bootstrappt Zwei-Account-Harness) → P2 Lauf-Integrität+SSRF (R4+R3) → P3 Quality-Gates-Wiring (`npm run test` als CI-Gate). P1 auf `change opened` gesetzt, Change-ID `testing-integration-security-gate`.

### Dateien

- `context/foundation/test-plan.md` — **neu**: phased Test-Rollout (§1–§8 schema-konform), 5 Risiken + Response-Guidance, 3 Phasen, §3-State-Tabelle (P1 `change opened`)
- `dtb-project/project-changelog/2026-06/2026-06-22.md` — diese Session

### Kontext

Nach Abschluss des OEJTS-MVP (S1) ist der nächste Kurs-Schritt Modul 3 (Tests). `/10x-test-plan` als stateful Orchestrator legt die Rollout-Strategie an und übergibt phasenweise an die `/10x-new` → research → plan → implement-Kette. Reihenfolge nutzt-gesteuert: P1 trifft beide genannten Top-Ängste (Key-Dichtheit, Cross-Tenant) zuerst.

### Naechste Schritte

- [ ] **`/clear` + kopierte Invocation einfügen:** `/10x-new testing-integration-security-gate` (+ Intent-Block für Rollout-Phase 1) → eröffnet den Change-Folder, dann `/10x-research`
- [ ] Optional: CLAUDE.md-Eintrag „kein Test-Runner eingerichtet" angleichen (vitest ist da) + `test_command` in `workflow.config.yaml` ist bereits `npm run test`
- [ ] Uncommittete Doku committen (test-plan.md, Changelog, WORKFLOW_STATUS) — kein Push-Zwang, reine `context/`/`dtb-project/`-Doku
