# 2026-06-24

## Session 1 - 06:27

### Implementiert

Test-Rollout **Phase 3 „Quality-gates wiring"** (context/foundation/test-plan.md §3) — kompletter 10x-Durchlauf `/10x-new` → `/10x-research` → `/10x-plan` → `/10x-implement` (3 Phasen):

- **Phase 1** (`99f6e52`): `npm run test` (Unit) als Step in den `ci`-Job — gated `deploy` via bestehendes `needs: ci`.
- **Phase 2** (`236a0a3`): neuer `integration`-Job (`supabase/setup-cli@v2` + `supabase start -x <slim set>`, env-Export nach `$GITHUB_ENV`, Wegwerf-`ENCRYPTION_KEY`, `npm run test:integration`); `deploy` → `needs: [ci, integration]`. CLI-Pin von angenommenen 2.23.4 auf **2.98.2** korrigiert (= Lockfile).
- **Phase 3** (`8db7c4d` + Epilogue `31e4dc8`): Doc-Closeout — test-plan §3 Status `complete`, §5 Gates, §6.3 e2e bewusst verworfen, §6.6 Phase-3-Notiz; lessons.md Required-Check-Regel. change.md → `implemented`.
- Branch-Protection auf `main` gesetzt (Required Checks `ci` + `integration`); 3.1 abgehakt (`5ca3a30`).
- Verifikations-Branch `testing-quality-gates-wiring` angelegt/gepusht (PR-Verifikation) und danach gelöscht.

### Dateien

- `.github/workflows/ci.yml` — `npm run test`-Step + `integration`-Job + `deploy: needs: [ci, integration]`
- `context/foundation/test-plan.md` — §3/§5/§6.3/§6.6 Closeout
- `context/foundation/lessons.md` — Regel „in-YAML needs vs. Required Status Check"
- `context/changes/testing-quality-gates-wiring/` — change.md, research.md, plan.md, plan-brief.md

### Kontext

Schließt die archivierte S-01-Lücke („CI-Lint skippt Deploy lautlos"): bis dato lief in CI **kein** Test, ein Logik-/RLS-/Auth-/Run-/SSRF-Regress deployte grün. Jetzt blocken Unit- **und** Integration-Tests den Deploy. Der eigentliche Sichtbarkeits-Teil (geskippter Deploy unsichtbar) ist über Branch-Protection als Required-Checks gelöst — YAML allein kann das nicht.

### BLOCKER (offen)

- **`integration`-Job auf `main` reproduzierbar ROT** (Läufe `31e4dc8` + `5ca3a30`): `ci` grün, `integration` scheitert an Schritt 8 `npm run test:integration`, `deploy` korrekt geskippt → **das Gate funktioniert**, aber die Tests laufen in CI nicht durch.
- **Leithypothese:** Der env-Export greift `ANON_KEY` (Legacy-JWT), aber das lokale Supabase/`.env.test` nutzt das **neue** `sb_publishable_…`-Key-Format → Supabase-Calls 401 → alle Tests rot. (Key-Namen `API_URL`/`ANON_KEY` per Docs bestätigt korrekt → es ist die Key-_Form_, nicht der Name.)
- Logs ohne Repo-Admin-Auth gesperrt (403). Warte auf **Fehler-Log Schritt 8** ODER lokale `npx supabase status -o env`-Ausgabe, um den exakten Publishable-Key-Namen zu pinnen, bevor ein Fix gepusht wird (jeder rote `main`-Push ist durch Branch-Protection jetzt teuer).

### Naechste Schritte

- [ ] Evidenz beschaffen: `integration` → Schritt 8 Fehlerzeilen ODER `npx supabase status -o env` lokal
- [ ] env-Export in `ci.yml` fixen (richtiger Publishable-Key statt Legacy-`ANON_KEY`), pushen, grün verifizieren
- [ ] Danach Change archivierungsreif → `/10x-archive testing-quality-gates-wiring`
