# Changelog 2026-06-18

## Session 1 - 05:45

### Implementiert

**S-04 `oejts-measurement-run`: Phase 1 deployed + committet, Phase 2 codiert (am Manual-Gate).**

- **Phase 1 abgeschlossen + committet** — Resume aus Session 4 (Code war grün, Migration stand aus):
  - **Prod-Migration** `20260617190000_runs.sql` angewandt (`! npx supabase db push`, von Damian
    autorisiert — der Auto-Mode-Classifier blockt das Auto-Confirm `y`) → Kriterium 1.1.
  - **Manual-Gate im Studio** (1.6–1.8): Lauf anlegbar (`pending`, `owner_id` via `auth.uid()`),
    Zwei-User-RLS (B sieht A's Lauf nicht), Löschen — alle grün. Progress 1.1–1.8 ✅.
  - **Zwei Commits:** `8c2fd52` (Planungsartefakte: plan/brief/plan-review/Instrument + Roadmap
    S-04 proposed), `6e396ec` (Phase-1-Code: Migration, OEJTS-Modul, reine Funktionen + 33 Tests,
    Service-CRUD, Types).
- **Phase 2 codiert** (`/10x-implement phase 2`) — alle Automated-Gates grün, **uncommitted, am Manual-Gate**:
  - **LLM-Client** `src/lib/llm/openai-compatible.ts`: `chatCompletion` → `POST {baseUrl}/chat/completions`,
    Bearer-Key, `redirect:"manual"`, `isPublicHttpsUrl`-Guard auf der baseUrl, 60s-AbortController-Timeout,
    Retry/Backoff bei 429/5xx (3 Versuche), **tolerantes jsonMode** (bei 400/422 einmal ohne
    `response_format` wiederholen), leakt nie Key/Header.
  - **`getDecryptedTarget`** um `modelName` erweitert (additiv; `test-connection` destrukturiert nur
    `baseUrl`/`apiKey` → unberührt).
  - **`processNextRepetition`** im runs-Service (Herzstück): decrypt → seedbar permutieren → Messages
    bauen → Call → parsen → `run_repetition` persistieren → Lauf-Aggregat (Tokens/Fehlquote) →
    Status-Übergang. **F3** (Modellkonfig weg → ganzer Lauf `failed` statt Exception), **F4**
    (unique-Verletzung `23505` bei Parallelaufruf → „bereits fortgeschritten", kein Fehler). `createRun`
    gibt bei unsichtbarer Persona/Konfig **`null`** (Route → 400 statt 500).
  - **API-Routen:** `/api/runs` (GET Liste / POST anlegen 201), `/api/runs/[id]` (GET 404 / DELETE 404,
    kein PUT), `/api/runs/[id]/step` (POST → `RunProgress`, 404).
  - **Gates:** Lint grün, Tests 33 grün, `astro check` 0 errors, Build grün.

### Dateien

- `supabase/migrations/20260617190000_runs.sql` — auf Prod appliziert (P1)
- `src/lib/llm/openai-compatible.ts` — **neu** (P2), OpenAI-kompatibler Chat-Completion-Client
- `src/lib/services/runs.ts` — `processNextRepetition` + `toStepState`-Mapper + `createRun`→`null` (P2)
- `src/lib/services/model-configs.ts` — `getDecryptedTarget` liefert zusätzlich `modelName` (P2)
- `src/pages/api/runs/{index,[id],[id]/step}.ts` — **neu** (P2), Start/Liste/Status/Abbruch/Step
- `context/changes/oejts-measurement-run/plan.md` — Progress 1.1–1.8 + 2.1–2.4 ✅

### Kontext

S-04 ist der Methodenkern (Stream B, Leitstern). Diese Session brachte ihn von „Phase-1-Code grün" auf
„Phase 1 live + committet, Phase 2 (der eigentliche LLM-Durchstich) codiert und am Manual-Gate". Nach
Phase 2 ist ein Lauf vollständig per API fahrbar; die `/runs`-UI ist Phase 3.

### Befunde / Gotchas

- **Studio-RLS-Test:** `set local request.jwt.claims` wirkt nur **innerhalb einer Transaktion** —
  Autocommit-Editor zerlegt sonst `set local` + Insert, `auth.uid()` wird `null`. Tests in
  `begin; … commit;` kapseln und als Ganzes ausführen.
- **Untypisierter Supabase-Client + strictTypeChecked:** `data` aus `.maybeSingle()` ist `any`;
  ein Cast wird mal als `no-unnecessary-type-assertion` (any bidirektional), mal beim Zugriff als
  `no-unsafe-*` geflaggt. Lösung: das `any` über einen **typisierten Funktionsparameter** lautern
  (Mapper `toStepState`, gleiches Muster wie `toView`) statt Cast + Property-Zugriff.
- **Prod-`db push`** weiterhin nur per User-`!`-Kommando (Classifier blockt Auto-Confirm).

### Naechste Schritte

- [ ] **Phase-2-Manual-Gate (2.5–2.10):** echter Endpunkt via `curl`/PowerShell ODER bequemer zusammen
  mit der Phase-3-UI abnehmen → dann **Phase-2-Commit** + SHA-Writeback in Progress
- [ ] **Phase 3** `/10x-implement oejts-measurement-run phase 3` (`/runs`-Seite, client-getriebener Step-Loop)
- [ ] Danach Slice abschließen: `/10x-impl-review` → Roadmap S-04 `done` → `/10x-archive` → Push
- [ ] Offen aus Vorsessions: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren, Test-User-Cleanup remote-DB

---

## Session 2 - 17:08

### Implementiert

**S-04 `oejts-measurement-run`: Phase-2-Manual-Gate (2.5–2.10) abgenommen, committet, gepusht + Deploy live verifiziert.**

- **Manual-Gate live gegen die echte OpenAI-API (`gpt-4o`) gefahren** — die `/runs`-UI ist erst
  Phase 3, daher die API direkt getrieben: lokaler Dev-Server (`npm run dev`, Prod-Supabase) +
  Playwright-Browser (Damian eingeloggt) → `fetch` aus der Session-Konsole (Cookie-Auth, umgeht die
  curl-/PKCE-Gotchas). Alle sechs Kriterien grün:
  - **2.5** Lauf N=3 → `pending→running→completed`; `run_repetitions`: 3× `ok`, `item_order` mit
    32 Einträgen, sauberes `{"answers":[…]}`-JSON, Tokens je 1099/420 (in Studio gegengeprüft).
  - **2.6** JSON-Mode live geparst (3/3 ok); Freitext-Fallback + `okCount=0` per Unit-Test (13 grün);
    „failed ohne Lauf-Abbruch" live über 2.7 demonstriert.
  - **2.7** Bad-Key-Config → `failedCount` 1→2, Lauf `failed` (0 verwertbar), 0 Tokens (401).
  - **2.8** Token-Aggregat 3297/1260 = exakte Summe der drei Wiederholungen.
  - **2.9** `running` → `DELETE` → 404, aus Liste weg (`on delete cascade`).
  - **2.10** SSRF: Create-Linie live (6 Vektoren — Nicht-HTTPS/localhost/loopback/Metadata/private
    Netze → 400) + Call-Linie (`isPublicHttpsUrl`, 8 Unit-Tests, `redirect:"manual"`).
- **Test-Läufe + temp-Configs wieder aufgeräumt** (0 verbleibende Läufe; nur „E2E Test OpenAI"-Config bleibt).
- **Progress 2.5–2.10 in `plan.md` auf `[x]`**, committet als `0348733`, **gepusht** → Cloudflare-Deploy.
- **Deploy live verifiziert:** `GET /api/runs` auf der Prod-Worker-URL → **HTTP 401** (Route existiert
  erst seit `cc2fff2`, Auth-Guard greift) ⇒ Phase-2-Code ist live, CI-Lint hat nicht geblockt.

### Dateien

- `context/changes/oejts-measurement-run/plan.md` — Progress 2.5–2.10 ✅ (`0348733`)

### Kontext

Damit ist **Phase 2 vollständig abgeschlossen** (Code `cc2fff2`, Gate-Abnahme `0348733`, beide live):
ein OEJTS-Lauf ist end-to-end per API fahrbar und auf Produktion erreichbar. Es fehlt nur noch die
Bedien-Oberfläche (Phase 3) und der Slice-Abschluss.

### Befunde / Gotchas

- **Lokales workerd: „Error: Network connection lost"** ist ein transienter Fehler beim
  fetch-Lesen (nicht beim Schreiben) — die DELETEs liefen durch, nur der abschließende GET warf.
  Retry-tolerant abfragen, dann sauber.
- **`gh` CLI nicht installiert + kein `GITHUB_TOKEN`** → Actions-Status nicht direkt abfragbar.
  Indirekter Deploy-Check über einen erst im neuen Commit existierenden Endpunkt (401 statt 404) funktioniert.
- **curl scheitert an der lokalen TLS-Interception** (Exit 35 / HTTP 000); `Invoke-WebRequest`
  (Windows-Cert-Store) klappt — aber PS 5.1 kennt **kein** `-SkipHttpErrorCheck`, Status über
  `$_.Exception.Response.StatusCode` holen.

### Naechste Schritte

- [ ] **Phase 3** `/10x-implement oejts-measurement-run phase 3` (`/runs`-Seite, client-getriebener Step-Loop)
- [ ] Danach Slice abschließen: `/10x-impl-review` → Roadmap S-04 `done` → `/10x-archive`
- [ ] Offen aus Vorsessions: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren, Test-User-Cleanup remote-DB

---

## Session 3 - 18:50

### Implementiert

**S-04 `oejts-measurement-run`: Phase 3 (UI) implementiert, Full-Plan-Impl-Review + Hardening, Slice archiviert + Roadmap `done`, gepusht (Deploy getriggert).**

- **Phase 3 — `/runs`-UI** (`/10x-implement … phase 3`), alle Automated-Gates grün (Lint, Build, `astro check` 0 errors), Manual-Gate 3.4–3.9 von Damian abgenommen:
  - **`RunRunner.tsx`** (neue Island): Start-Formular (Persona-/Modellkonfig-Select, N=1–25 Default 5), client-getriebener Step-Loop über `setTimeout`-Verkettung (kein `setInterval`), Live-Fortschrittspanel mit Abbrechen, Lauf-Liste mit Status-Badge/Fehlquote/Tokens. Spiegelt `PersonaCatalog`/`ModelConfigManager` (Modul-Scope-`redirectToSignin()`, `messageFromPayload`, Re-Fetch, `loadError`-Banner).
  - **`runs.astro`**: server-seitiger Initial-Load (`listRuns` + `listPersonas` + `listModelConfigs` via `Promise.all`, RLS-gescoped, `loadError`-Fallback), Island `client:load`.
  - **`middleware.ts`**: `/runs` zu `PROTECTED_ROUTES`. **`dashboard.astro`**: „Läufe →"-Link.
  - Commits: `2f3ba29` (Phase 3), `7ffefbf` (Epilogue: change → `implemented`).
- **Full-Plan-Impl-Review** (`/10x-impl-review`, 2 Parallel-Agenten): Verdikt **APPROVED**. Plan-Treue über alle 15 Dateien vollständig (keine Drift/Missing/Out-of-Scope). 0 critical, 3 warnings, 3 observations.
  - **Gefixt (`c07761b`):** F1 — `cancelActive()` (mit Bestätigung) von `remove()` getrennt, gemeinsamer `deleteRunRequest()`; F2 — leeres N-Feld fängt `NaN` → `MIN_REPS` (kein `value={NaN}`); F4 — Token-Aggregat zählt jetzt alle real verbrauchten Tokens (auch failed-Reps mit `usage`).
  - **Geskippt:** F3 (Server-Timeout deckt in-flight-Abbruch), F5 (Resume plan-konform v2-Scope), F6 (rep_index-CHECK unkritisch, Migration live).
  - Report: `reviews/impl-review.md`; `change.md` → `impl_reviewed`.
- **Slice abgeschlossen:** Roadmap S-04 → `done` (Tabelle + Detail + `## Done`-Eintrag), `/10x-archive` → `context/archive/2026-06-17-oejts-measurement-run/` (`d06afbe`). `git mv` lief am Windows-Lock auf → `Move-Item` + `git add` erfasste den Rename (5 Dateien) sauber.
- **Push** `05e695b..d06afbe` auf `main` (von Damian via `!`-Kommando — Auto-Mode-Classifier blockt Prod-Deploy-Push).

### Dateien

- `src/components/runs/RunRunner.tsx` — **neu**, Runner-Island (Start + Step-Loop + Liste)
- `src/pages/runs.astro` — **neu**, geschützte Seite mit SSR-Initial-Load
- `src/middleware.ts` — `/runs` geschützt · `src/pages/dashboard.astro` — Läufe-Link
- `src/lib/services/runs.ts` — F4-Fix (Token-Aggregat statusunabhängig)
- `context/archive/2026-06-17-oejts-measurement-run/` — archivierter Slice (inkl. `reviews/impl-review.md`)
- `context/foundation/roadmap.md` — S-04 `done` + `## Done`-Eintrag

### Kontext

Damit ist **S-04 (Methodenkern, Stream B, Leitstern) end-to-end fertig**: `/runs` erlaubt Lauf starten → live durchlaufen → Status/Fehlquote/Tokens. Der Slice ist reviewt, gehärtet, archiviert, gepusht und **live verifiziert**: `/runs` auf Prod → **HTTP 302 → `/auth/signin`** (neue Route existiert, Auth-Guard greift; vor dem Deploy lieferte sie 404).

### Befunde / Gotchas

- **`git mv` scheitert am Windows-File-Lock** (`Permission denied`) → Fallback `Move-Item` + `git add <alt> <neu>`; Git erkennt es trotzdem als Rename (History bleibt).
- **Push auf `main` blockt der Auto-Mode-Classifier** (Prod-Deploy) → User-`!`-Kommando, wie bei Prod-`db push`.
- **`$host` ist in PowerShell reserviert** → in Schleifen `$h` o. ä. nutzen.
- **Prod-Worker-URL:** `persona-forge.damian-spyra-ai.workers.dev` (mit `-ai`).

### Naechste Schritte

- [x] **Deploy verifiziert:** `/runs` auf Prod → 302 `/auth/signin` (Phase 3 live)
- [ ] **S-05 `distribution-results`** (Leitstern-Fortsetzung): Achsen-Aggregation/Verteilung/Typ je Achse — Prerequisite S-04 ✅. Start mit `/10x-plan distribution-results`
- [ ] Offen aus Vorsessions: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren, Test-User-Cleanup remote-DB

---

## Session 4 - 20:55

### Implementiert

**S-05 `distribution-results`: geplant (`/10x-plan`) + Phase 1 implementiert (Scoring-/Aggregations-Kern + Service + API) — Automated-Gates grün, uncommitted am Manual-Gate.**

- **Planung** (`/10x-plan distribution-results`): Komplexität MEDIUM, 8 Design-Fragen in 2 Runden (alle Empfehlungen übernommen). Entscheidungen: eigene Route `/runs/[id]`, Mean+SD+Roh-Verteilung, Modaltyp+Achsen-Split+Konsistenz-%, **achsen-weiser Dropout** bei unparsed-Items, leichtgewichtige CSS/SVG-Visualisierung (keine Chart-Library), **on-the-fly-Berechnung** (keine Migration/keine persistierten Aggregate), Roh-Score mit Cutoff-Linie, Warnbanner bei <2 verwertbaren Läufen. Artefakte: `change.md`/`plan.md`/`plan-brief.md`. 2-Phasen-Schnitt (Kern+Service+API → UI).
- **Phase 1 codiert** (`/10x-implement … phase 1`), alle Automated-Gates grün:
  - **`oejts-score.ts`** (neu): `scoreAxes` (je Achse `constant + Σ(sign·value)`, **Dropout** → Achse `null` wenn ein Item unparsed), `deriveType` (4-Buchstaben-Typ, `null` bei Lücke), `axisScale` (min/max/cutoff je Achse aus Vorzeichen-Extrema — alle vier 8..40, cutoff 24).
  - **`oejts-aggregate.ts`** (neu): `aggregateRun` → je Achse Mean/Populations-SD/Roh-Verteilung/Buchstaben-Häufigkeit/`usableCount`; laufweit Modaltyp (achsenweise Mehrheit, Tie-Break über Mean-Seite zum Cutoff), Typ-Konsistenz (Anteil vollständiger Wiederholungs-Typen == Modaltyp), `usableReps`. Param auf `Pick<RunRepetition,"item_values">[]` verengt (kein erfundenes Feld beim Service-Mapping).
  - **`types.ts`**: `AxisDistribution`, `RunAggregate`, `RunResultView` (`state: ready|empty|unfinished`).
  - **`runs.ts`**: `getRunResult` (RLS-gescoped via `getRun` + `run_repetitions`-Load, on-the-fly `aggregateRun`; `pending`/`running` → `unfinished`, 0 verwertbar → `empty`). Mapper `toRepForScoring` lautert das `any` des untypisierten Clients.
  - **`api/runs/[id]/result.ts`** (neu): `GET` → `getRunResult` (404 wenn nicht sichtbar).
  - **Tests:** 15 neue (Scoring 9 + Aggregation 6): Referenzwert (alle 3 → ISFJ, Achsen am Cutoff 24), Cutoff-Grenzfall (24→low/25→high), Dropout senkt nur betroffene Achse, Tie-Break, leeres Aggregat, N=1, Determinismus. Gesamt **48 Tests grün**.
  - **Gates:** Lint clean, `npm run test` 48 grün, `astro check` 0 errors, Build grün.

### Dateien

- `src/lib/runs/oejts-score.ts` + `.test.ts` — **neu**, reine Scoring-Funktionen + Tests
- `src/lib/runs/oejts-aggregate.ts` + `.test.ts` — **neu**, Lauf-Aggregation + Tests
- `src/types.ts` — `AxisDistribution`/`RunAggregate`/`RunResultView`
- `src/lib/services/runs.ts` — `getRunResult` + `toRepForScoring`-Mapper
- `src/pages/api/runs/[id]/result.ts` — **neu**, Ergebnis-Endpunkt
- `context/changes/distribution-results/` — Planungsartefakte (change/plan/plan-brief)

### Kontext

S-05 ist die Leitstern-Fortsetzung von S-04: Rohantworten → Verteilung je Achse + Typ-Stabilität (FR-016, US-01). Phase 1 ist der deterministische Auswertungskern (die testbarste Stelle, Roadmap-Risk) plus Service/API — ohne UI. Keine Migration nötig, da die Rohdaten seit S-04 in `run_repetitions` liegen. Nach Phase 1 ist das Ergebnis vollständig per API abrufbar; Phase 2 baut die Ergebnisseite `/runs/[id]`.

### Befunde / Gotchas

- **Skalengrenzen je Achse berechnen, nicht global annehmen:** die Achsen-Konstanten (30/12/30/18) sind so kalibriert, dass alle vier Achsen auf 8..40 mit Cutoff-Mitte 24 landen — aber `axisScale` leitet das je Achse aus den Item-Vorzeichen-Extrema her (robust gegen ein zweites Instrument).
- **Lint:** Non-Null-Assertion (`.find(...)!`) in Tests ist verboten (`@typescript-eslint/no-non-null-assertion`) → Helper mit explizitem `throw`. Prettier-Reflow via `lint:fix`.
- **`userId`-unused-Hint** in `processNextRepetition` ist pre-existing aus S-04 (astro check zählt es als Hint, 0 errors).

### Naechste Schritte

- [ ] **Phase-1-Manual-Gate (1.5–1.8):** `GET /api/runs/[id]/result` gegen einen abgeschlossenen Lauf (plausible Verteilung + Modaltyp), unparsed-Items → reduzierte `usableCount`, `unfinished`/`empty`-States, Zwei-User-RLS 404 → dann **Phase-1-Commit** + SHA-Writeback. Bequem gebündelt mit Phase 2 abnehmbar.
- [ ] **Phase 2** `/10x-implement distribution-results phase 2` (Ergebnisseite `/runs/[id]` + CSS/SVG-Verteilung + Verlinkung)
- [ ] Danach Slice abschließen: `/10x-impl-review` → Roadmap S-05 `done` → `/10x-archive` → Push
- [ ] Offen aus Vorsessions: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren, Test-User-Cleanup remote-DB

---

## Session 5 - 21:11

### Implementiert

**S-05 `distribution-results`: Phase 2 (UI) codiert — Ergebnisseite `/runs/[id]` + CSS-Verteilung + Verlinkung. Automated-Gates grün, uncommitted; Phase 1 + Phase 2 gemeinsam am Manual-Gate.**

- **`RunResult.tsx`** (neue Island): Typ-Stabilitäts-Panel (Modaltyp als Mono-Headline + „Stabilität X %" + Fehlquote/Tokens), je Achse eine Karte mit **leichtgewichtigem CSS-Histogramm** (Score-Skala `scale.min..max`, gestrichelte **Cutoff-Linie**, je Score-Wert eine gestapelte Punkt-Säule, Mittelwert-Markierung, Pol-Beschriftung `low (min) … Cutoff … (max) high`), Mean/SD-Text, Buchstaben-Häufigkeit als Badges, **„nicht belastbar"-Banner** bei `usableCount < 2` (je Achse) bzw. `usableReps < 2` (laufweit). Edge-States `unfinished` (noch nicht abgeschlossen) und `empty` (keine verwertbaren Antworten, Fehlquote ausgewiesen).
- **`runs/[id].astro`** (neue dynamische Route): Server-Load via `getRunResult` (RLS-gescoped), `loadError`-Banner, `client:load`-Island. **404 über `Astro.response.status`** statt Top-Level-`return` (siehe Gotcha).
- **`RunRunner.tsx`**: „Ergebnis"-Link (BarChart3) je Lauf-Zeile auf `/runs/${run.id}` (Aktionsleiste mit Löschen gebündelt; aktiver Lauf ohne Link).
- **Gates:** Lint clean, `astro check` 0 errors (70 Dateien), Build grün. (Phase-1-Tests weiterhin 48 grün.)

### Dateien

- `src/components/runs/RunResult.tsx` — **neu**, Ergebnis-Island (Typ-Panel + Achsen-Histogramme + Edge-States)
- `src/pages/runs/[id].astro` — **neu**, geschützte Ergebnisseite mit SSR-Load
- `src/components/runs/RunRunner.tsx` — „Ergebnis"-Link je Zeile

### Kontext

Phase 2 macht das in Phase 1 berechnete Ergebnis sichtbar: `/runs/[id]` zeigt je Achse die Verteilung (Mean/SD + Roh-Verteilung mit Cutoff-Linie) und den Modaltyp + Stabilität — der Leitstern-Output „Verteilung statt Punktwert" (FR-016, US-01). Damit ist S-05 code-vollständig; es fehlt nur die gebündelte Manual-Abnahme (Phase 1 + 2) und der Slice-Abschluss.

### Befunde / Gotchas

- **ESLint-Crash bei `return` im Astro-Frontmatter:** die getypte Regel `@typescript-eslint/no-misused-promises` wirft „Expected node to have a parent" an einem Top-Level-`return` im Frontmatter (`return new Response(...)`/`return Astro.redirect(...)`). Keine bestehende Seite nutzt das (Redirects liegen in der Middleware). **Workaround:** 404 via `Astro.response.status = 404` + Not-Found-Zustand im Template rendern — kein Top-Level-`return`.
- **Nur `button.tsx` als shadcn-Komponente** vorhanden (kein card/badge/alert) → Ergebnis-UI mit Plain-Tailwind im bestehenden cosmic-Stil (gespiegelt von `RunRunner`).
- `/runs/[id]` ist durch die bestehende Middleware (`startsWith("/runs")`) automatisch geschützt — keine Middleware-Änderung nötig.

### Naechste Schritte

- [ ] **Manual-Gate (gebündelt Phase 1 + 2):** 2.4–2.11 (UI: Achsen-Verteilung/Cutoff, Typ-Panel, <2-Banner, empty/unfinished, Link, Logout-Redirect, Zwei-User-RLS) + 1.5–1.8 (API; großteils via UI miterledigbar, 1.6 braucht Studio-Eingriff in `item_values`). Dann **zwei getrennte Commits** (Phase 1, Phase 2) + SHA-Writeback.
- [ ] Slice abschließen: `/10x-impl-review` → Roadmap S-05 `done` → `/10x-archive` → Push (Prod-Deploy via User-`!`-Kommando)
- [ ] Offen aus Vorsessions: F6 Trigger-Idempotenz, Husky/lint-staged-Hook reparieren, Test-User-Cleanup remote-DB
