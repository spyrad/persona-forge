# Changelog 2026-06-15

## Session 1 - 05:50

### Implementiert
- **S-01 Phase 3 abgeschlossen** (`a210f0c`) — Manual-Gate geschlossen:
  - 3.3: DevTools-Network auf `/auth/signin` (ausgeloggt) zeigt nur
    document/script/css/png + Vite-HMR-Websocket, **kein** `…supabase.co/auth/v1/…`
    → Middleware-Optimierung greift (öffentliche Route ohne Auth-Roundtrip).
  - 3.5: Browser-Login → `/dashboard` zeigt User-E-Mail (`md.motion.value@gmail.com`).
  - 3.4 war bereits per curl belegt (`4ca2744`).
- **S-01 Phase 4 (Live-Deploy + lokale Verifikation):**
  - Push auf `main` (`4a2048c..a210f0c`) → **CI grün, beide Jobs** (ci + deploy),
    Run `27521878004`. Prod-Sanity (curl): `/auth/signin` 200, `/dashboard` → 302
    `/auth/signin`. (4.1 ✓)
  - 4.2 lokal: frischer Signup (User `98cbe7ba…`) → Login → Dashboard → Signout. ✓
  - 4.3 lokal: `select * from public.profiles` zeigt neue UUID
    `98cbe7ba-3d80-4dd2-8708-fe26c1d2d4fd` (Trigger gefeuert). ✓
- **Bug gefunden + gefixt — E-Mail-Bestätigungs-Callback** (`4b5e916`):
  - Befund bei der Prod-Confirm-Runde: Mail-Link landet auf
    `localhost:3000/?code=…` → `ERR_CONNECTION_REFUSED`.
  - Root Cause (systematisch): (A) `signup.ts` ruft `signUp` **ohne**
    `emailRedirectTo` → Supabase nutzt die Dashboard-Site-URL (Scaffold-Default
    `localhost:3000`); (B) **kein** Code-Exchange-Handler — `src/pages/auth/` hatte
    nur signin/signup/confirm-email, nirgends `exchangeCodeForSession`.
  - Warum 4.2 trotzdem „grün": User wurde im Studio **manuell** bestätigt
    (Workaround seit 2026-06-14), der echte Link-Pfad lief nie.
  - Fix: neue Route `src/pages/auth/callback.ts` (PKCE `exchangeCodeForSession`
    → `/dashboard`, Fehler → `/auth/signin?error=…`) + `emailRedirectTo =
    ${origin}/auth/callback` in `signup.ts` (origin-basiert, Dev :4321 + Prod ohne
    Hardcoding). Build grün; Callback-Fehlerpfade per curl belegt (ohne code →
    „Missing confirmation code"; bogus code → PKCE-Verifier-Fehler, beweist die
    `exchangeCodeForSession`-Verdrahtung).

### Dateien
- `src/pages/auth/callback.ts` — **neu**, PKCE-Code-Exchange-Handler
- `src/pages/api/auth/signup.ts` — `emailRedirectTo` (origin-basiert) in `signUp`
- `context/changes/email-auth-live/plan.md` — Progress 3.3/3.4/3.5 + 4.1/4.2/4.3 `[x]`
- `context/changes/email-auth-live/change.md` — `updated: 2026-06-15`

### Kontext
Resume mitten in S-01 Phase 3 (manuelle Checks offen). Phase 3 abgeschlossen,
auf `main` deployt, lokale Phase-4-Checks grün. Bei der ersten **echten**
Mail-Link-Bestätigung (statt Studio-Workaround) fiel der kaputte Confirm-Flow auf
— systematisch diagnostiziert (kein Code-Bug bei der transienten miniflare
„Network connection lost"-Confirm-Seite; echter Bug beim Mail-Link-Redirect).
User-Entscheidung: richtiger Callback statt Confirm abschalten/vertagen.

### Befunde / Gotchas
- **miniflare-Dev-Quirk:** transienter „Network connection lost"
  (`node_modules/miniflare/.../entry.worker.js`) bei in-flight-Request während
  Vite-`program reload` (Dependency-Optimierung beim Erst-Treffer). Per F5 grün,
  in Prod unmöglich (kein miniflare). Kein Code-Bug.
- **Astro-Watcher-Race (Windows):** `astro dev` crasht gelegentlich beim
  Anlegen neuer Routen (`stat` auf `*.ts.tmp.*` der atomaren Write-Operation) —
  Dev-Server neu starten, kein Code-Problem.

### Naechste Schritte
- [ ] **Supabase URL-Configuration (Damian):** Site URL → Prod-Workers-URL;
  Redirect-Allowlist → `http://localhost:4321/auth/callback` +
  `https://persona-forge.damian-spyra-ai.workers.dev/auth/callback`
- [ ] Lokal neu verifizieren: frischer Signup → Mail-Link → `/auth/callback` →
  `/dashboard` eingeloggt (kein `localhost:3000` mehr)
- [ ] Dann `4b5e916` auf `main` pushen (Deploy) → Prod-Runde **4.4** (voller Flow)
  + **4.5** (kein Auth-Roundtrip auf `/auth/signin`)
- [ ] Danach S-01 abschließen + `email-auth-live` archivieren (`/dtb:archive`)
- [ ] Cleanup: Test-User aus remote-DB — `7ad40ffa…514794` (Phase 2),
  `98cbe7ba…2d4fd` (4.2 heute) + ältere `+pf…`-Aliases; `.playwright-mcp/` in
  `.gitignore`
- [ ] OEJTS-Quelle fixieren (Owner: Damian) — blockt S-04
- [ ] Test-Runner (Vitest) einrichten, `test_command` in `workflow.config.yaml`

---

## Session 2 - 17:40

### Kontext
Resume nach Damians Supabase-URL-Config. Ziel: Mail-Link verifizieren + Prod 4.4/4.5.
Dabei einen versteckten Deploy-Blocker gefunden und behoben — S-01 Phase 4 komplett.

### Implementiert / Verifiziert
- **S-01 Phase 4 abgeschlossen (4.4 + 4.5 ✅)** — S-01 `email-auth-live` Status `complete`.
- **Lokaler Voll-Confirm-Flow verifiziert** (echter Mail-Link, nicht Studio-Workaround):
  frischer Signup (`pf…1742`) → Bestätigungsmail → Verify-Token → `/auth/callback`
  → E-Mail bestätigt → Login → `/dashboard` („Welcome, …pf…1742"). Beweist
  `emailRedirectTo`→`/auth/callback`, korrekte Supabase-Allowlist und den
  `exchangeCodeForSession`-Pfad.
- **PKCE-Cross-Browser-Verhalten bestätigt:** Mail-Link in anderem Browser als der
  Signup → `?error=PKCE code verifier not found`; E-Mail trotzdem confirmed, danach
  normaler Login. Gnädiger Fallback → als Backlog-Idee (UX) festgehalten.

### Bug gefunden + gefixt — CI-Deploy-Blocker (`5025edf`)
- **Befund:** `/auth/callback` lieferte auf **Prod 404**, obwohl `4b5e916` (Callback-Route)
  gepusht war. Root Cause (systematisch via GitHub-Actions-API): CI-Run `27522902004`
  (HEAD `09909c5`) **failure** an `npm run lint` — 2 `prettier/prettier`-Fehler in
  `callback.ts` (umgebrochene Redirect-Strings). `deploy` hängt an `needs: ci` → still
  übersprungen → Prod blieb auf `a210f0c` (ohne Callback-Route).
- **Warum durchgerutscht:** Pre-Commit-Hook (husky/lint-staged `eslint --fix`) hat die
  Format-Fehler bei `4b5e916` nicht gegriffen — Hook-Setup als offene Aufgabe notiert.
- **Fix:** `npm run lint:fix` (reine Formatierung, Verhalten identisch) → Lint 0 + Build 0
  lokal → committet + gepusht (`5025edf`) → Run `27559204315` **ci+deploy success**.
- **Prod-Verifikation nach Deploy:** `/auth/callback` ohne code → `302 …?error=Missing
  confirmation code`; bogus code → `302 …?error=PKCE…`; `/auth/signin` 200; `/dashboard`
  ohne Session → `302 /auth/signin`.

### Dateien
- `src/pages/auth/callback.ts` — prettier-konform (CI-Lint-Blocker entfernt)
- `context/changes/email-auth-live/plan.md` — 4.4/4.5 `[x]` + 4.1 Re-Deploy-Note
- `context/changes/email-auth-live/change.md` — `status: complete`
- `BACKLOG.md` — Cross-Device-Confirm + Custom-SMTP als Ideen
- `WORKFLOW_STATUS.md` — Phase 4 komplett, Gotchas (CI-Lint-blockt-Deploy, PKCE-selber-Browser)

### Befunde / Gotchas
- **CI-Lint blockt Deploy lautlos:** Roter `npm run lint` → `deploy` skippt (kein Alarm).
  Nach Push auf `main` immer den **deploy-Job** prüfen, nicht nur „push ok".
- **Supabase-SMTP-Rate-Limit:** ~2–4 Mails/h Free Tier → nach 3–4 Signups blockiert;
  frischer Prod-Mail-Confirm-Happy-Path daher vertagt (User-Entscheidung, Evidenz reicht).

### Naechste Schritte
- [ ] `email-auth-live` archivieren (`/dtb:archive` bzw. `/10x-archive`) → S-01 schließen
- [ ] Husky/lint-staged-Hook reparieren (Format-Fehler vor Commit abfangen)
- [ ] Cleanup Test-User remote-DB (`pf…1742`, `pf174558b`, ältere `+pf…`) + `.playwright-mcp/` in `.gitignore`
- [ ] (Optional) Frischer Prod-Mail-Confirm wenn SMTP-Limit zurückgesetzt
