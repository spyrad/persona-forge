# 2026-08-05

## Session 1 - 06:44

### Implementiert

**Beide Zertifizierungs-Formulare abgesendet — alle drei Badges beantragt, im selben
Termin. Kein Code-Diff.**

- **Formular 1 (Builder, M1–3) abgesendet** — Bestaetigungsseite belegt. Inhalt:
  Repo-Link (public), Prod-URL, Typ „Wlasny projekt", Zgoda „Tak", fuenf
  Pflicht-Screenshots (Login, Dashboard, Runs-Startformular als Dateneingabe,
  HEXACO-Verteilung als Datendarstellung, CI-Testlauf) plus vier Anhaenge und ein
  Kommentar auf Polnisch (Methodenkern, drei Kernentscheidungen, der
  Findings-statt-Noten-Umbau als groesste Huerde).
- **Neu erzeugt fuer das Test-Feld:** `builder-9-test-ci.png` (CI-Job `ci`, Schritt
  `npm run test`: 31 Dateien / 289 Tests gruen) und `builder-10-test-integration.png`
  (Job `integration`, `npm run test:integration`: 9 Dateien / 81 Tests gegen echtes
  Supabase, darunter `rls-cross-tenant` mit 27). Der zuerst versuchte E2E-Lauf
  scheiterte, weil lokales Supabase seit dem 19.07. gestoppt ist und Docker nicht
  laeuft — CI-Screenshots waren der bessere Weg, weil sie die Pipeline-Einbettung
  gleich mitzeigen.
- **Formular 2 (Architect + Champion, M4/M5) abgesendet** — „Obie odznaki", Report als
  **PDF (2 Seiten) + Markdown-Quelle**, Projektwahl „Pipeline CI/CD do review kodu",
  sechs Champion-Screenshots, Kommentar zu M4 und M5.
- **PDF-Konvertierung ohne neue Abhaengigkeit:** `context/architect-report.md` →
  `.ressources/certyfikacja/architect-report.pdf` via remark/rehype (aus Astros
  Baum, inkl. `remark-gfm` fuer Tabellen) + headless Chromium aus `playwright`.
  Erster Durchlauf ergab 3 Seiten; Layout enger gezogen (9 pt, 11 mm Rand) → 2
  Seiten, passend zum „two-pager" der Aufgabe. Node loest ESM-Importe relativ zum
  **Skript** auf, nicht zum cwd — das Skript musste dafuer im Projektbaum liegen.

### Fehler in dieser Session (dokumentiert, weil lehrreich)

Nach dem Befuellen von Formular 1 habe ich auf „abgeschickt" hin das zweite Formular
im selben Tab geoeffnet — obwohl ich selbst gewarnt hatte, dass ein Seitenwechsel den
Stand verwirft. Es war noch nicht abgesendet, alle Eingaben waren weg. Wiederherstellung
war vollstaendig moeglich (Texte im Verlauf, Dateien lokal), kostete aber ~15 Minuten.
Lehre: bei nicht wiederherstellbaren UI-Zustaenden nicht auf die Aussage vertrauen,
sondern den Zustand selbst pruefen (Bestaetigungsseite sehen), bevor man navigiert.

Zweiter Fund am Rande: Baserow oeffnet ueber „Eine Datei hinzufuegen" ein **In-Page-Modal**
mit eigenem „Hochladen"-Knopf; der Klick direkt in die gestrichelte Dropzone loest
dagegen sofort den Dateidialog aus — nur dieser Weg ist per Playwright praktikabel.

### Dateien

- `.ressources/certyfikacja/{builder-9-test-ci.png, builder-10-test-integration.png,
architect-report.pdf}` — NEU (gitignored)
- `.ressources/certyfikacja/README.md` — Zuordnung auf die echten Formularfelder
- Repo-Code unveraendert

### Kontext

Damit ist der Kurs-Block abgeschlossen: 10xBuilder, 10xArchitect und 10xChampion sind
im 2. Termin (10.08.) beantragt, Nachreichen ist ausgeschlossen und auch nicht noetig.
Bewertung laeuft nach dem Termin, Feedback-Frist zwei Wochen (bis ca. 24.08.);
Stille bedeutet laut Kursdokument „bestanden".

### Naechste Schritte

- [ ] **Feedback abwarten** — bis ca. 24.08.; Plattform-Nachrichten im Blick behalten (Demo-Day-Einladungen gehen individuell raus)
- [ ] **Neues Feature** — Empfehlung #8 SD3 (Registry-Umbau kommt aus HEXACO), Lizenz Jones & Paulhus 2014 vorab verifizieren
- [ ] **Ideen-Inbox** (offen): #1, #6, #7, #8 SD3, #9 HEXACO-100, #10 Repo-LICENSE
- [ ] Optional: im Playwright-Fenster abmelden, Docker Desktop beenden
