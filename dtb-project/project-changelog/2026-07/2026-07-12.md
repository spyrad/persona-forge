# 2026-07-12

## Session 1 - 21:33

### Durchgeführt

Session ohne Code-Änderung — Beratung zur Anthropic-Model-Config für die
Baseline-Läufe (Schritt 1.4, läuft bei Damian) + Diagnose eines
Connection-Test-Fehlers.

- **Anthropic-Model-Config geklärt:** Base URL `https://api.anthropic.com/v1`
  (OpenAI-Compatibility-Layer; Client baut `{base}/chat/completions`, Trailing-
  Slash egal), `model_name` exakt ohne Datums-Suffix: `claude-opus-4-8`,
  `claude-sonnet-5`, `claude-haiku-4-5` (günstigstes für viele Reps),
  `claude-fable-5` (teuer, Thinking immer an). Empfehlung: mehrere Configs
  (Haiku/Sonnet/Opus) = direkt 3 Modelle für den späteren Compare.
- **401 beim „Test connection" diagnostiziert — Key und Config sind korrekt:**
  Der Test probt `GET {base}/models` mit `Authorization: Bearer`
  (`test-connection.ts:27`). Anthropics Compat-Layer deckt nur
  `/chat/completions` ab; das native `/v1/models` verlangt `x-api-key` → 401
  auf Bearer. Die echten Läufe (POST `/chat/completions`, Bearer) funktionieren.
  Spiegelbild des z.ai-Gotchas („/models-Test grün, Läufe 429"). Workaround:
  Save ohne Test, Beweis per Mini-Baseline-Lauf (1–2 Reps).
- **Vorschlag (Entscheidung offen):** Connection-Test-Fallback — bei 401 auf
  `/models` einen minimalen `POST /chat/completions`-Probe (max_tokens 1).
  Als Minor geparkt.

### Dateien

- Keine Änderungen (reine Beratung; `.superpowers/` weiterhin untracked/lokal).

### Kontext

Damian fährt die Baseline-Läufe (1.4) und legte dafür eine Anthropic-Config an;
der irreführende 401 hätte die Config fälschlich als kaputt erscheinen lassen.
Model Compare bleibt bei 10/17 — Phase-3-Sichtprüfung + ENTSCHEIDUNGSPUNKT
warten auf die Baseline-Daten.

### Naechste Schritte

- [ ] **1.4 fortsetzen:** Anthropic-Config speichern (Save, Test ignorieren) → Mini-Baseline-Lauf als Beweis → je Modell ≥ 5 Reps
- [ ] **Sichtprüfung + ENTSCHEIDUNGSPUNKT Phase 4** — sobald Baseline-Daten da sind
- [ ] Entscheiden: Connection-Test-Fallback (Minor) — parken oder umsetzen
- [ ] Champion: `/10x-archive ci-review-agent` + grüne Läufe PR #3–#7 in `evidence.md` (vor ~07.10.)
