# 2026-07-01

## Session 1 - 05:49

### Implementiert

Kurs **Modul 4 (10xArchitect) zu Ende gebracht** — von der L4-Archivierung über den
letzten Artefakt L5 (DDD) bis zum einreichbereiten Architektur-Report. Reine Analyse-,
Doku- und Kurs-Arbeit, **kein App-Code geändert**.

- **L4 abgeschlossen — `/10x-archive refactor-opportunities`:** Pre-Flight sauber (keine
  uncommitteten/staged Reste), Status `impl_reviewed`, alle Progress-Items `[x]` mit SHA,
  beide Reviews vorhanden. Kein Roadmap-Match. `git mv` →
  `context/archive/2026-06-30-refactor-opportunities/`, `change.md` gestampft (`status: archived`,
  `archived_at`). Commit `5906197`, gepusht → CI `ci`/`integration`/`deploy` grün, Prod 200.
- **L5 (s04e05 DDD) — 3 Domänen-Artefakte via Sub-Agenten**, jeweils von mir als Rezensent
  am Code gegengeprüft (tragende Claims per Grep/Read verifiziert). Lektionsmethode „deleguj
  Agentowi" + „entdecken & wählen, nichts vorwegnehmen" befolgt:
  - `01-domain-distillation.md` — ~27 Begriffe Ubiquitous Language, Subdomänen Core/Supporting/
    Generic, 9 Aggregat-Kandidaten, 7 Model-vs-Code-Rozjazdy. Verifiziert: `permute`-Flag
    (`types.ts:192`) toter Vertrag, `RELIABLE_MIN=2` (`axis-chart.tsx:14`) nur UI, API
    `repetitionCount.min(1)`.
  - `02-invariant-aggregate-refactor.md` — **Invariante #1 = Belastbarkeit (= D1)**; Design
    `RunAggregate.reliability` + `MIN_RELIABLE_REPS` + `UnreliableRunError`. Der Agent
    **widerspricht** begründet dem Distillation-Design (separates `reliability`-Feld statt
    `state:'unreliable'`, weil `state` ein Render-Diskriminator ist).
  - `03-anti-corruption-layer.md` — schlimmster Leak = Supabase-Client (5 Schichten/6 Dateien,
    `SupabaseClient`-Typ 4× dupliziert, `"23505"` in `runs.ts:423`), **ehrlich als mild**
    dimensioniert; `openai-compatible.ts` als sauberes ACL-Vorbild. Voll akkurat verifiziert
    (`@supabase` in `src/components` = 0).
- **Modul-Abschluss — `context/architect-report.md`:** Two-Pager L2–L5 (Badge-Deliverable),
  Sektion 6 „Entscheidungen, die mir gehören" in Damians Stimme aus dem Session-Verlauf.
- **Report-Sprache geprüft & auf Polnisch umgestellt:** Kursregeln
  (`.ressources/infos/certyfikacja-10xarchitect-*`) verlangen **keine feste Sprache**, aber
  Reviewer sind polnisch + eigene Konvention „AI-course projects = Polish". Report nach PL
  übersetzt (Code-Identifier + `Datei:Zeile`-Belege unverändert), zwei Sprachpolituren.
- **Memory aktualisiert:** `persona-forge-modul4-architect` + Index auf „Modul 4 KOMPLETT
  (4/4), Report PL einreichbereit".

### Dateien

- `context/archive/2026-06-30-refactor-opportunities/` (neu, via `git mv` aus `context/changes/`)
- `context/domain/01-domain-distillation.md` · `02-invariant-aggregate-refactor.md` ·
  `03-anti-corruption-layer.md` (neu)
- `context/architect-report.md` (neu → dann PL) — Zertifizierungs-Deliverable
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` — Modul 4 4/4 komplett
- Commits: `5906197` (archive) · `db6cad5` (L5+Report) · `63a7ee6` (Report-PL) · `ba4848f` (PL-Politur)
- Keine `src/`-Änderung.

### Kontext

L5 = pragmatisches DDD auf dem bestehenden Code: Ubiquitous Language destillieren, den
kern-nächsten + schwächst-erzwungenen Invarianten (D1/Belastbarkeit) zum Aggregat-Wächter-Plan
formen und den schlimmsten Abhängigkeits-Leak (Supabase) als ACL benennen — alles nur als
Diagnose/Plan, kein Refaktor. Damit sind alle **4 Architect-Artefakte** fertig und der
Architektur-Report ist einreichbereit. D1 ist wie in L4 vorgesehen als benanntes Domänen-Konzept
gelandet (Scoring-Yield-Quote vs. Antwort-Fehlquote). Die L5-Pläne 02/03 sind fertige Inputs für
künftige `/10x-plan`-Changes (post-MVP-Zyklus).

### Naechste Schritte

- [ ] **3 lokale Commits pushen** (`db6cad5`/`63a7ee6`/`ba4848f`) — reine Doku; Push = Prod-Deploy;
      `origin/main` steht noch auf `5906197`.
- [ ] **Termin-Entscheidung 2026-07-02** — jetzt rein strategisch (Architect einreichbereit): 5. Juli (Auszeichnung + Demo-Day) vs. 10. Aug (entspannt, ohne Auszeichnung); kein Nachreichen.
- [ ] **Architektur-Report einreichen** — `context/architect-report.md` ins Zert-Formular
      (baserow.io), erscheint in der letzten Kurswoche.
- [ ] **Optional (L5-Kür):** Event Storming via `event-storming-canvas` für den Run-Flow.
- [ ] **Optional:** Modul 5 (10xChampion) als nächster Block, falls gewünscht.

---

## Session 2 - 14:46

### Implementiert

Zwei zusammenhängende Prod-Themen um `ENCRYPTION_KEY` gelöst: erst das fehlende
Worker-Secret (F3-Follow-up), dann ein dadurch ausgelöster Krypto-Key-Mismatch, der alle
Läufe scheitern ließ. Systematische Diagnose (Symptom → Code-Pfad → Wurzel), kein Rätselraten.

- **F3-Follow-up — `ENCRYPTION_KEY`-Sync:** Prod zeigte Banner „ENCRYPTION*KEY is not
  configured — model configurations cannot be saved" (`config-status.ts:17`). Ursache: der
  Deploy-Job in `.github/workflows/ci.yml` synct nur `SUPABASE*\*`/`SENTRY_DSN`in den Worker,
**nicht**`ENCRYPTION_KEY`. Fix: `ENCRYPTION_KEY`in`secrets:`-Liste **und** `env:`-Block
des `wrangler-action`-Steps aufgenommen (`93fae0c`). GitHub-Secret gesetzt, gepusht → Deploy
  grün, **Banner-Vorkommen 0** im Prod-HTML verifiziert (Prod 200).
- **Incident — Läufe scheitern „Something went wrong" / hängen „Läuft 0/5, 0 Tokens":**
  Diagnose ergab **AES-GCM-Key-Mismatch**. `getDecryptedTarget` (`model-configs.ts:122-138`)
  ruft `decryptApiKey` **außerhalb** des try/catch von `processNextRepetition`
  (`runs.ts:348`); wirft der Decrypt, fliegt der Fehler → Step 500, **kein Rep geschrieben** →
  Lauf hängt bei 0 (provider-unabhängig, weil vor dem LLM-Call).
- **Wurzel:** `SUPABASE_URL` = **gehostete** Supabase-DB (`lccaundrniuievkmusko.supabase.co`),
  von Prod **und** lokalem Dev geteilt. Die Modellkonfigs wurden mit dem **`.dev.vars`-Key**
  verschlüsselt; der von mir vorher gesetzte **Zufalls-Key** konnte sie nicht entschlüsseln.
  Mein „frischer Key ist gefahrlos" war falsch — Ciphertext existierte längst in der geteilten DB.
- **Fix:** GitHub-Secret `ENCRYPTION_KEY` auf den **`.dev.vars`-Wert** überschrieben (User),
  Re-Deploy via Push `70784f7` → grün. `.env` == `.dev.vars` verifiziert (beide 44 Zeichen,
  identisch). Zielzustand: `.dev.vars` = `.env` = GitHub-Secret identisch.

### Dateien

- `.github/workflows/ci.yml` — `ENCRYPTION_KEY` in Deploy-`secrets:`+`env:` (`93fae0c`)
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` — F3 abgehakt, Meilenstein + Gotcha
- Commits: `93fae0c` (ci) · `70784f7` (Status/F3). GitHub-Secret `ENCRYPTION_KEY` = `.dev.vars`-Key.
- Keine `src/`-Änderung (Fehler war Konfig/Secret, nicht Code).

### Kontext

Der `ENCRYPTION_KEY` verschlüsselt LLM-API-Keys at rest (`crypto.ts`, AES-256-GCM). Da Prod und
lokaler Dev **dieselbe** gehostete Supabase-DB teilen, MUSS der Prod-Key identisch zum
`.dev.vars`/`.env`-Key sein — sonst sind bereits gespeicherte Konfigs unentschlüsselbar und jeder
Lauf scheitert vor dem ersten Rep. Ein rotierter/abweichender Key verwaist den Ciphertext.

### Naechste Schritte

- [ ] **User-Verifikation:** 2 hängende „Läuft 0/5"-Läufe löschen, frischen Lauf starten → sollte
      jetzt durchlaufen. Falls weiter Fehler: Secret-Wert auf Whitespace/Zeilenumbruch prüfen.
- [ ] **Härtung (optional, Backlog-Kandidat):** `getDecryptedTarget`-Wurf im Step als benannten
      Fehlerpfad behandeln statt 500 (Lauf sauber `failed` + `repError` statt Hänger).
- [ ] **Termin-Entscheidung 2026-07-02** (Architect einreichbereit).

---

## Session 3 - 15:26

### Implementiert

Neues Feature via dtb-Workflow gebaut: **Modellname-Vorschläge aus dem Verbindungstest**.
`/dtb:feature-plan` → Spec, dann (User-Wahl) **direkt implementiert** (impl-plan/plan-review
bewusst übersprungen, kleiner vertikaler Slice). Der Verbindungstest holt bereits `GET /models`,
nutzte aber nur den Count — jetzt landen die Modell-IDs im „Modellname"-Feld als Vorschläge.

- **Server** (`test-connection.ts`): `probeModels` gibt zusätzlich `models: string[]` zurück
  (nur IDs, kein Key-Leak) via neuem reinen Helper `extractModelIds` (tolerant, dedupliziert,
  sortiert).
- **UI** (`ModelConfigManager.tsx`): `modelOptions`-State aus der Test-Response; „Modellname"
  als native **`<datalist>`** (durchsuchbar, Freitext bleibt). Liste invalidiert bei Änderung
  von Base-URL/Key. `FormField` um optionales `list`-Prop erweitert.
- **Ansatz-Entscheidung:** native `<datalist>` statt shadcn `Command`/`Popover` → **kein neuer
  Dependency**, native Suche, Freitext-Fallback by design. Trade-off: browser-natives Popup-Styling.
- **Verifiziert:** Unit-Tests **65/65** grün (inkl. 5 neue `extractModelIds`), `npm run build`
  (Typecheck) + Lint grün. Deploy `e1aeb37` gepusht (CI/Deploy in Verifikation, Monitor).

### Dateien

- `src/lib/llm/model-list.ts` (neu) — `extractModelIds`-Helper
- `src/lib/llm/model-list.test.ts` (neu) — 5 Unit-Tests
- `src/pages/api/models/test-connection.ts` — `models` im `ProbeResult`
- `src/components/auth/FormField.tsx` — optionales `list`-Prop
- `src/components/models/ModelConfigManager.tsx` — `modelOptions` + `<datalist>`
- `dtb-project/project-workflows/features/FEATURE_MODEL_NAME_COMBOBOX.md` (neu) — Status „Fertig zum Testen" (nicht im BACKLOG)
- Commit `e1aeb37`

### Kontext

Beim Anlegen einer Provider-Connection kannte man die exakten Modellnamen bisher nicht — man
musste sie blind tippen. Die Daten waren serverseitig schon da (nur als Count „127 Modelle"
genutzt); das Feature reicht sie additiv durch und bietet sie nativ an. Bewusst klein gehalten,
verhaltenserhaltend (Formularlogik unangetastet, `models` rein additiv).

### Naechste Schritte

- [ ] **Live-Abnahme:** nach Deploy `/models` → Connection testen → prüfen, ob Modellnamen als
      Vorschläge erscheinen (tippen filtert), Auswahl+Speichern funktioniert, Freitext weiter möglich.
- [ ] Bei Abnahme: Feature-Spec → **Abgenommen**, optional BACKLOG-Eintrag + `/dtb:archive`-Kandidat.
- [ ] **Termin-Entscheidung 2026-07-02** (Architect einreichbereit).

---

## Session 4 - 20:43

### Implementiert

Zwei Prod-Themen in einem Rutsch: erst die scheiternden **z.ai-Läufe diagnostiziert**
(kein App-Bug, sondern Provider-Konfig + Reasoning-Tempo), dann daraus **zwei Features**
über den superpowers-Flow (Brainstorm → Spec → Plan → Subagent-Driven-Development mit
Task-/Final-Review) gebaut, gereviewt und deployt. Beide live, Prod 200.

- **z.ai-Debug (systematisch am echten API-Verhalten):**
  - „Alle Tests scheitern" → Verbindungstest (`GET /models`) war grün, nur echte Läufe
    (`POST /chat/completions`) failten. Ursache 1: der z.ai-Key ist ein **GLM-Coding-Plan-Key**
    → auf dem Standard-Endpunkt `paas/v4` + bezahltem Modell **429 „insufficient balance"**.
    Fix (User): Base URL auf `https://api.z.ai/api/coding/paas/v4/`. Live verifiziert (200).
  - „Sehr lange, kein Ergebnis" → Ursache 2: GLM reasont per Default (~9–16 s, riesiges
    `reasoning_content`) → Cloudflare-Edge-Zeitlimit. `thinking:{type:"disabled"}` → ~2,8 s +
    sauberes JSON (reasoning_tokens 0), live per curl belegt.
- **Feature 1 — Lauf-/Wiederholungs-Timing** (7 Tasks SDD, jede Task-reviewed, Final-Review
  „merge-ready" auf Opus): Migration `duration_ms`+`finished_at`; Messung via `performance.now()`
  in `processNextRepetition`; `finalize`-Helfer setzt `finished_at`; reiner Helfer `summarizeTiming`
  (Modell-Zeit = Σ Rep-Dauern, Wall-Clock = finished−created); additive DTOs
  (`RunResultView.timing`, `RunView.finishedAt`, `RunProgress.lastRepDurationMs`); UI: Ergebnis
  (Datum + Dauer + ⌀/Rep), Runner-Live (letzte Rep + laufende Summe), Datum in der Liste. Migration
  vom User auf die gehostete DB gespielt.
- **Feature 2 — z.ai `thinking:disabled`** (1 Task SDD): Host-Gate `isZaiEndpoint(baseUrl)` →
  additives `thinking:{type:"disabled"}` im `chatCompletion`-Body, nur für z.ai (OpenAI unberührt);
  fetch-Stub-Test deckt beide Pfade am echten Request-Body ab.
- **Verifikation:** Unit 73/73 → 77/77 grün, Build grün, beide CI-Runs `ci`+`integration`+`deploy`
  grün, Prod 200. Integration-itest (Timing-Persistenz) im CI grün.

### Dateien

- `supabase/migrations/20260701230000_run_timing.sql` (neu) — `duration_ms` + `finished_at`
- `src/lib/runs/run-timing.ts` (+`.test.ts`, neu) — `summarizeTiming`/`formatDuration`/`formatDateTime`
- `src/lib/services/runs.ts` — Messung, `finalize`, `lastRepDurationMs` (6 Returns), `getRunResult`-Timing
- `src/lib/runs/run-schemas.ts` · `src/types.ts` — additive DTO-Felder + `RunTiming`
- `src/components/runs/RunResult.tsx` · `RunRunner.tsx` — Timing-Anzeigen (Ergebnis/Live/Liste)
- `src/test/integration/run-integrity.itest.ts` — Persistenz-Fall (duration_ms/finished_at)
- `src/lib/llm/openai-compatible.ts` (+`.test.ts`, neu) — `isZaiEndpoint` + thinking-Wiring
- `docs/superpowers/{specs,plans}/2026-07-01-*` — 2 Specs + 2 Pläne
- Commits: Timing `31e9383`→`dfe0170`, thinking `a8753de`→`c5631f0`. `origin/main = c5631f0`.

### Kontext

Der Nutzer sah nur „Tests scheitern" ohne Fehlermeldung (Läufe zeigen `error` nirgends im UI —
Produkt-Mangel, als separater Vorgang geparkt). Die Diagnose lief strikt Symptom → Code-Pfad →
Wurzel und wurde jeweils per curl am realen z.ai-Verhalten belegt, statt zu raten. Die zwei
Features fielen direkt aus der Debug-Runde: „wie lange dauert ein Lauf" (Timing) und „warum hängt
z.ai" (thinking). z.ai-Gotchas ins Projektgedächtnis gesichert (`persona-forge-zai-provider.md`).

### Naechste Schritte

- [ ] **Live-Abnahme beider Features:** z.ai-Lauf (glm-5.2, Coding-Endpunkt) → sollte in ~2–3 s/Rep
      durchlaufen UND Timing anzeigen (Live + Ergebnis + Liste).
- [ ] **Optional (geparkt):** Fehler-Durchreichung + pro-Rep-Fehleranzeige im UI (der „ich sehe die
      Fehler nicht"-Mangel) — eigener Vorgang.
- [ ] **Optional (Mikro-Härtung):** `isZaiEndpoint` auf `=== "z.ai" || endsWith(".z.ai")` (verhindert
      theoretische `evilz.ai`-Treffer; kein realer Angriffsweg, da User-eigene Config).
- [ ] **Termin-Entscheidung 2026-07-02** (aus S1, weiter offen).
