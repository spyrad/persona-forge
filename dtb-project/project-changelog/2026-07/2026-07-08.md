# 2026-07-08

## Session 1 - Modul-5-Fortschritt: SDK-Entscheidung + s05e03

### Implementiert

Reine Kursarbeit + Entscheidung (Champion-Pfad). Kein `src/`-Code, kein Commit.

- **SDK-Entscheidung getroffen und bestätigt: Vercel AI SDK 6** (assemble-Weg). Der
  offene Blocker „SDK final bestätigen" aus S1/2026-07-06 ist damit erledigt.
- **Kostenanalyse geschärft (Vercel vs. Claude Agent SDK):** Nicht „SDK vs. API"
  entscheidet die Kosten, sondern der Auth-Pfad/Plan. Beide Wege können marginale
  Per-Token-API-Kosten vermeiden — Claude Agent SDK via Max-Subscription
  (`CLAUDE_CODE_OAUTH_TOKEN`, aber Wochen-Limits/Terms in CI zu prüfen), Vercel AI
  SDK via **z.ai Coding-Plan** (`/coding/paas/v4`, Flat-Fee). Damit bleibt
  Vercel + z.ai kostenseitig konkurrenzfähig UND behält Modell-Freiheit.
- **s05e03 „Code Review w erze AI: standardy, DoD i Agent w pipeline" komplett
  quergelesen** (inkl. Deep Dive). Kern-Erkenntnisse:
  - GHA-Grundlagen (workflow/trigger/job/step/action/runner); minimaler
    `review.yml` auf `pull_request`+`workflow_dispatch`; Secret statt Klartext-Key.
  - **Composite Action** = Agent als wiederverwendbares Plugin (`action.yml`,
    `using: composite`, `inputs`/`outputs`); Start lokal in `.github/actions/`,
    fremde Actions an `@<sha>` pinnen (Security).
  - **Referenz-`requirements.md` ist unser Blueprint:** PR-Titel + Body + `git diff`
    als Input, 6 Kriterien 1–10, Side-Effects = PR-Kommentar + Labels
    `ai-cr:passed`/`ai-cr:failed`, **On-demand-Re-Run via Label `ai-cr:review`**
    (= der „nach Bedarf abschaltbar/triggerbar"-Toggle, kurstreu). Diff-Gotcha:
    `checkout` braucht `fetch-depth: 0`.
  - **ToolLoopAgent (Vercel AI SDK)** als „Scorer" (`Output.object`, null Tools);
    später Tools ergänzen (`readPlan`, `postPrComment`). Kosten-Bramka
    `stopWhen: stepCountIs(N)` + `onStepFinish` (Token-Messung); `isLoopFinished()`
    ohne Limit = Budget-Risiko in CI.
  - **promptfoo** als Regressions-Gate (providers × tests; `is-json`, `llm-rubric`,
    `javascript`-Score-Schwelle), Modelle via OpenRouter — Kurs vergleicht sogar z.ai/glm.
  - Deep Dive: `10x-impl-review-ci` trennt Mechanik (`SKILL.md`) von Kriterien
    (`references/`); Eigenbau-Weg reused nur die Kriterien via Tool.

### Entscheidungen

- **Vercel AI SDK 6 (assemble) final gewählt** — Gründe: Modell-Freiheit (z.ai/GLM),
  volle Kontrolle über `Output.object`-Schema + `score`-Bramka, max. Deckung mit dem
  M5L3-Kursweg, Lernwert (Tool-Loop-Mechanik selbst bauen). Ehrlicher Vorbehalt:
  die Lektion rät „starte mit dem Gotowiec (Claude Code Action), Eigenbau erst wo er
  limitiert" — bewusst als Trade-off akzeptiert.
- **Nächster Zug:** erst s05e03 lesen (erledigt), dann `/10x-new`. Vor dem
  `/10x-new` optional die 5–6 Review-Kriterien festlegen (Aufgabe 1 der Lektion).

### Dateien

- **Neu:** `dtb-project/project-changelog/2026-07/2026-07-08.md` — dieser Log
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` — SDK-Entscheidung eingetragen,
  nächster Schritt aktualisiert

### Kontext

Zwischen s05e02 (SDK-Grundlage) und dem praktischen Bau lag die offene SDK-Wahl. Diese
Session hat sie entschieden (Vercel AI SDK 6) und mit s05e03 das CI-/HITL-Muster + die
Champion-Beweis-Struktur geklärt. Damit ist der Bau des CI-Review-Agenten planungsreif.

### Naechste Schritte

- [ ] **Optional: 5–6 Review-Kriterien festlegen** (Aufgabe 1 der Lektion, je „1"- und
      „10"-Zustand) — Input für die `requirements.md`.
- [ ] **`/10x-new` für Change „CI-Review-Agent"** anlegen + `requirements.md`
      (Kriterien, PR-Kommentar + Labels, `ai-cr:review`-Toggle) → `/10x-research` →
      `/10x-plan` → implementieren.
- [ ] **promptfoo-Regressions-Gate** aufsetzen (z.ai/GLM + 1–2 Vergleichsmodelle via
      OpenRouter, 1 komplexer Diff mit bekannten Fehlern).
- [ ] **Champion-Beweise** (Pipeline-View, Job-Logs, PR-Kommentar) in L3 sammeln.
- [ ] **Optional:** CI-Review-Agent als Feature in `BACKLOG.md` aufnehmen.

---

## Session 2 - 21:56 — Change „ci-review-agent" angelegt + `/10x-research`

### Implementiert

Kein `src/`-Code. 10x-Workflow-Artefakte für das Champion-Projekt.

- **`/10x-new` → Change `ci-review-agent`** angelegt (`context/changes/ci-review-agent/change.md`,
  `status: preparing`). Slug auf kebab-case normiert (`CI-Review-Agent` → `ci-review-agent`).
  `change.md` fixiert den Blueprint (6 Kriterien, PR-Kommentar + Labels
  `ai-cr:passed`/`ai-cr:failed`, `ai-cr:review`-Toggle, Baustufen 1–3).
- **`/10x-research`** mit 3 parallelen Sub-Agenten (Konventionen / LLM-Pipeline /
  Prior-Art) → `context/changes/ci-review-agent/research.md`. Drei Kernbefunde:
  1. **Reviewer = „semantischer Linter" für das, was ESLint NICHT sieht.**
     `strictTypeChecked` + Prettier + `react-compiler` + `jsx-a11y` decken
     Typ/Format/Hooks/a11y mechanisch ab. LLM-Mehrwert nur bei projektspezifischen
     Regeln: Farb-Tokens, **RLS-Vollständigkeit** neuer Tabellen, API-Quartett
     (`prerender=false` + uppercase-Export + zod + `requireUser`),
     `client:load`-Sinn, Code-Platzierung (`services/`/`types.ts`).
  2. **SDK-Frage wieder offen (Kern-Fork für `/10x-plan`).** `chatCompletion()`
     (`src/lib/llm/openai-compatible.ts:142`) ist reiner fetch-Client, unter
     plain `tsx` wiederverwendbar, z.ai-`thinking:disabled`-Toggle schon
     eingebaut. Hausstil = null-SDK. → Vercel AI SDK 6 (`Output.object`,
     kurstreu) vs. Hausstil (`chatCompletion({jsonMode:true})` + zod-`safeParse`).
  3. **Kriterien-Quelle liegt im Repo:**
     `.claude/skills/10x-impl-review-ci/references/impl-review-instructions.md`
     (6–7 Dimensionen + Severity×Impact + Fix-Grammatik) + `workflow-template.yml`
     (Label-Gate, `fetch-depth:0`, Verdict als separater Commit-Status).
     Projekt-Risiken kanonisch in `context/foundation/test-plan.md`.
- **Runtime-Gotcha dokumentiert:** `review.ts` läuft außerhalb Astro →
  `astro:env/server` bricht unter tsx → Config aus `process.env` (neue Vars);
  DB/Krypto-Pfad (`getDecryptedTarget`, RLS-Session-gebunden) NICHT nutzbar.

### Entscheidungen

- **SDK-Entscheidung bewusst wieder geöffnet:** Der Research-Befund
  (`chatCompletion` tsx-tauglich, Hausstil null-SDK) stellt das im WORKFLOW_STATUS
  „bestätigte" Vercel AI SDK 6 legitim zur Debatte. Wird in `/10x-plan` final
  entschieden — nicht mehr als gesetzt behandelt.

### Dateien

- **Neu:** `context/changes/ci-review-agent/change.md` — Identity, `status: preparing`
- **Neu:** `context/changes/ci-review-agent/research.md` — Codebase-Grounding (Kriterien + Bau)

### Kontext

Der Bau des CI-Review-Agenten war planungsreif, aber ungeerdet. Diese Session hat
den Change formal eröffnet und den Codebase durchleuchtet: was der Reviewer prüfen
soll (die Nicht-Linter-Regeln), was `review.ts` erben kann (`chatCompletion`) und
welche fertigen Kriterien-Quellen im Repo liegen. Damit sind die zwei letzten
Urteilsfragen isoliert: SDK-Wahl + die 5–6 konkreten Kriterien.

### Naechste Schritte

- [ ] **Die 5–6 Review-Kriterien festlegen** (je „1"/„10"-Zustand, gegroundet in
      research.md Abschnitt A/D) — Input für `requirements.md` (Aufgabe 1 der Lektion).
- [ ] **SDK-Fork entscheiden:** Vercel AI SDK 6 vs. Hausstil (`chatCompletion` + zod).
- [ ] **`/10x-plan ci-review-agent`** — beide Fragen im Plan festzurren.
- [ ] promptfoo-Regressions-Gate (später, Baustufe 3).
