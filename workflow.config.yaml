project_name: "persona-forge"
namespace: "pf"

paths:
  workflows: "dtb-project/project-workflows"
  changelog: "dtb-project/project-changelog"
  rules: "dtb-project/project-rules"

repos:
  - name: "persona-forge"
    path: "."
    type: "astro"
    # Kein Test-Runner im Starter enthalten — setzen sobald eingerichtet (z. B. Vitest)
    test_command: null
    build_command: "npm run build"
    lint_command: "npm run lint"
    dev_command: "npm run dev"
